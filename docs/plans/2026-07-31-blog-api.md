# Plan — Bước 1 nối API: cụm Blog + nền `lib/api`

> **For agentic workers:** REQUIRED SUB-SKILL: dùng
> `superpowers:subagent-driven-development` (khuyến nghị) hoặc
> `superpowers:executing-plans` để thi công task-by-task. Step dùng checkbox
> (`- [ ]`).

**Goal:** Trang `/blog`, `/blog/[slug]`, rss, sitemap và teaser Journal trên
Home đọc dữ liệu THẬT từ API oRPC (9 bài seed từ mock đã duyệt), trên nền
`lib/api` dùng chung cho mọi bước nối sau — theo
[spec](../specs/2026-07-31-blog-api-design.md) và
[ADR-0016](../adr/0016-web-data-layer.md).

**Architecture:** Client `OpenAPILink` typed từ `@tourism/contract` (không
codegen); Server Component fetch + map DTO→VM trong `lib/api/posts.ts`; ISR
`revalidate=300` + cache-tag; tri-state `settle()`/`LoadErrorState`; markdown
render bằng `react-markdown` trong Typeset preset `reading`.

**Tech Stack:** Next 16.2.11 · React 19 · `@orpc/client` + `@orpc/openapi-client`
**ghim 1.14.8** · zod 4 (từ contract) · react-markdown + remark-gfm · Vitest
(2 project node/jsdom, ADR-0014) · Prisma seed (swc).

## Global Constraints (áp cho MỌI task)

- **Branch `feat/blog-api`** từ `main`; Conventional Commits, KHÔNG AI attribution.
- **Comment code + JSDoc: tiếng Việt.** Identifier tiếng Anh.
- **Copy user-facing: tiếng Anh, nằm trong `@tourism/i18n`** — không inline.
- **Tokens-only, không hex.**
- **TDD logic thuần** — test trước, ≥80% trên logic mới. Test component theo
  ADR-0014 (project `dom`, file `*.spec.tsx`).
- **KHÔNG tạo `loading.tsx` nào** (bẫy soft-404 — xem
  [`docs/conventions/soft-404-loading-tsx.md`](../conventions/soft-404-loading-tsx.md)).
- **KHÔNG đụng `apps/api/prisma/migrations/`** — kể cả comment.
- **API chạy ở cổng 3001** (`pnpm --filter @tourism/api dev`), web dev/build ở
  3000. Trước `next build`/`pnpm gate`: kiểm cổng 3000 bằng
  `curl -s -o /dev/null -w "%{http_code}" --max-time 3 http://localhost:3000/`
  (`000` = trống). Không kill bằng `pkill -f`; tra PID qua `ss -ltnp | grep ':3000'`.
- **`pnpm gate` từ nay cần API sống** (ADR-0016 §Hệ quả). `pnpm test:int`
  không chạy được ở máy này — ghi nợ cho CI.
- Import nội bộ apps/web **không đuôi** (`./slug` chứ không `./slug.js`) —
  Turbopack không map đuôi .js (comment ở `lib/toc.ts:2`).

---

### Task 1: Deps + `lib/api/env.ts`

**Files:**
- Modify: `apps/web/package.json`
- Create: `apps/web/src/lib/api/env.ts`
- Test: `apps/web/src/lib/api/env.spec.ts`
- Modify: `apps/web/.env.example` (tạo nếu chưa có) + `apps/web/.env.local` (local, không commit)

**Interfaces:**
- Produces: `apiOrigin(): string` — origin API cho phía server (ưu tiên
  `API_URL`, fallback `NEXT_PUBLIC_API_URL`, cuối cùng `http://localhost:3001`);
  `resolveApiOrigin(env: { API_URL?: string; NEXT_PUBLIC_API_URL?: string }): string`
  — lõi thuần để test.

- [ ] **Step 1: Cài deps** (chạy ở root repo)

```bash
pnpm --filter @tourism/web add @tourism/contract@workspace:* @orpc/client@1.14.8 @orpc/openapi-client@1.14.8 react-markdown remark-gfm
```

Kiểm `apps/web/package.json`: 3 gói orpc/contract đúng version ghim `1.14.8`
(không dấu `^`) — lệch version với API là lỗi. `react-markdown`/`remark-gfm`
theo caret mặc định.

- [ ] **Step 2: Test fail cho `resolveApiOrigin`**

`apps/web/src/lib/api/env.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { resolveApiOrigin } from './env';

describe('resolveApiOrigin', () => {
  it('ưu tiên API_URL (server-side) trước NEXT_PUBLIC_API_URL', () => {
    expect(
      resolveApiOrigin({ API_URL: 'http://api:3001', NEXT_PUBLIC_API_URL: 'http://pub:3001' }),
    ).toBe('http://api:3001');
  });
  it('fallback NEXT_PUBLIC_API_URL rồi mới tới default localhost:3001', () => {
    expect(resolveApiOrigin({ NEXT_PUBLIC_API_URL: 'http://pub:3001' })).toBe('http://pub:3001');
    expect(resolveApiOrigin({})).toBe('http://localhost:3001');
  });
  it('chuỗi rỗng coi như không khai (nền tảng deploy gửi "" khi ô bỏ trống)', () => {
    expect(resolveApiOrigin({ API_URL: '', NEXT_PUBLIC_API_URL: '' })).toBe(
      'http://localhost:3001',
    );
  });
  it('cắt dấu / cuối để ghép path không thành //', () => {
    expect(resolveApiOrigin({ API_URL: 'http://api:3001/' })).toBe('http://api:3001');
  });
});
```

- [ ] **Step 3: Chạy để thấy fail** —
`pnpm --filter @tourism/web test -- src/lib/api/env.spec.ts` → FAIL (module chưa có).

- [ ] **Step 4: Viết `env.ts`**

```ts
/**
 * Nguồn env DUY NHẤT của tầng dữ liệu web (ADR-0016 §6) — Nexora lặp base-URL
 * thủ công ở 8 file, đây là chỗ sửa bài học đó. Giá trị là ORIGIN TRẦN
 * (không kèm /api): path đã nằm trong contract.
 */
export function resolveApiOrigin(env: {
  API_URL?: string;
  NEXT_PUBLIC_API_URL?: string;
}): string {
  // Chuỗi rỗng là "không khai" — cùng lý do parseEnv của API strip chuỗi rỗng
  // (CLAUDE.md §Gotchas: nền tảng deploy gửi "" khi ô bị bỏ trống).
  const raw = env.API_URL || env.NEXT_PUBLIC_API_URL || 'http://localhost:3001';
  return raw.replace(/\/+$/, '');
}

/** Origin API cho code đang chạy (server ưu tiên API_URL; browser chỉ thấy NEXT_PUBLIC_*). */
export function apiOrigin(): string {
  return resolveApiOrigin({
    API_URL: process.env.API_URL,
    NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL,
  });
}
```

- [ ] **Step 5: Test pass** — chạy lại lệnh Step 3 → PASS.

- [ ] **Step 6: Env files.** `apps/web/.env.example` (commit):

```bash
# Origin API oRPC — origin TRẦN, không kèm /api (ADR-0016 §6)
NEXT_PUBLIC_API_URL=http://localhost:3001
# Server-side override (deploy nội bộ có thể trỏ URL private); dev để trống
API_URL=
```

`apps/web/.env.local` (KHÔNG commit) cùng nội dung. Nhắc: script pnpm chỉ tự
đọc `.env.local` (quy ước 19/07); Next tự nạp `.env.local` của `apps/web`.

- [ ] **Step 7: Commit** — `feat(web): deps oRPC client + module env tầng dữ liệu`

---

### Task 2: `lib/api/client.ts` + `lib/api/tags.ts`

**Files:**
- Create: `apps/web/src/lib/api/client.ts` · `apps/web/src/lib/api/tags.ts`
- Test: `apps/web/src/lib/api/tags.spec.ts` · `apps/web/src/lib/api/fetch-options.spec.ts`

**Interfaces:**
- Consumes: `apiOrigin()` (Task 1).
- Produces: `api` — client typed
  `JsonifiedClient<ContractRouterClient<typeof contract>>`, mọi call nhận
  context `{ next?: { revalidate?: number; tags?: string[] } }`;
  `TAGS = { POSTS: 'posts' } as const`; `postTag(slug: string): string` (dạng
  `post:<slug>`); `withNextOptions(init, context): RequestInit` (thuần).

- [ ] **Step 1: Test fail cho tags + withNextOptions**

`tags.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { TAGS, postTag } from './tags';

describe('cache tags', () => {
  it('taxonomy cố định', () => {
    expect(TAGS.POSTS).toBe('posts');
  });
  it('postTag sinh tag theo slug, một định dạng duy nhất', () => {
    expect(postTag('eating-your-way-through-hoi-an')).toBe('post:eating-your-way-through-hoi-an');
  });
});
```

`fetch-options.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { withNextOptions } from './client';

describe('withNextOptions', () => {
  it('gắn next.revalidate + tags từ client context vào RequestInit', () => {
    const init = withNextOptions({ method: 'GET' }, { next: { revalidate: 300, tags: ['posts'] } });
    expect(init).toMatchObject({ method: 'GET', next: { revalidate: 300, tags: ['posts'] } });
  });
  it('không context thì trả init nguyên vẹn (không thêm field next rỗng)', () => {
    expect(withNextOptions({ method: 'GET' }, undefined)).toEqual({ method: 'GET' });
  });
});
```

- [ ] **Step 2: Chạy thấy fail**, rồi **Step 3: viết code**.

`tags.ts`:

```ts
/**
 * Taxonomy cache-tag DUY NHẤT của web (ADR-0016 §3) — mỗi fetch public gắn tag
 * từ đây để bước on-demand revalidation (sau bước nối 1–4) chỉ còn thêm
 * endpoint, không phải đi cắm lại tag. Mẫu theo lib/revalidate.ts của Nexora.
 */
export const TAGS = {
  POSTS: 'posts',
} as const;

/** Tag theo từng bài — bust một bài không đụng cache list. */
export function postTag(slug: string): string {
  return `post:${slug}`;
}
```

`client.ts`:

```ts
import { createORPCClient } from '@orpc/client';
import type { JsonifiedClient } from '@orpc/openapi-client';
import { OpenAPILink } from '@orpc/openapi-client/fetch';
import type { ContractRouterClient } from '@orpc/contract';
import { contract } from '@tourism/contract';
import { apiOrigin } from './env';

/** Context per-call: Server Component điều khiển Next Data Cache qua đây. */
export interface ApiClientContext {
  next?: { revalidate?: number; tags?: string[] };
}

/** Thuần để test: trộn tuỳ chọn Next cache của context vào RequestInit. */
export function withNextOptions(
  init: RequestInit,
  context: ApiClientContext | undefined,
): RequestInit & { next?: ApiClientContext['next'] } {
  return context?.next ? { ...init, next: context.next } : init;
}

/**
 * Link OpenAPI (KHÔNG phải RPCLink): API mount contract theo path REST qua
 * @orpc/nest nên client phải nói chuyện bằng đúng các path đó (ADR-0016 §1).
 * Timeout 10s mặc định — Nexora không có timeout ở đâu cả, đây là điểm vá.
 */
const link = new OpenAPILink<ApiClientContext>(contract, {
  url: apiOrigin(),
  fetch: (request, init, { context }) =>
    globalThis.fetch(request, {
      ...withNextOptions(init ?? {}, context),
      signal: AbortSignal.timeout(10_000),
    }),
});

export const api: JsonifiedClient<ContractRouterClient<typeof contract, ApiClientContext>> =
  createORPCClient(link);
```

⚠️ Chữ ký chính xác của option `fetch`/`url` và generic context: đối chiếu
docs oRPC 1.14 qua context7 (`/dinwwwh/orpc`, mục "OpenAPI Link") NGAY khi
viết — nếu API thật khác (ví dụ `url` phải kèm base path, hay tham số thứ ba
không có `context`), sửa theo docs và ghi chú lại trong PR; phần
`withNextOptions` thuần không đổi.

- [ ] **Step 4: Test pass** cả hai spec. Typecheck:
`pnpm --filter @tourism/web typecheck` → 0 lỗi (xác nhận generic
`ContractRouterClient` khớp version 1.14.8).

- [ ] **Step 5: Commit** — `feat(web): oRPC OpenAPILink client + taxonomy cache-tag`

---

### Task 3: Khuôn tri-state — `resilience.ts` + `LoadErrorState` + i18n

**Files:**
- Create: `apps/web/src/lib/api/resilience.ts` ·
  `apps/web/src/components/feedback/load-error-state.tsx`
- Test: `apps/web/src/lib/api/resilience.spec.ts` ·
  `apps/web/src/components/feedback/load-error-state.spec.tsx`
- Modify: `libs/shared/i18n/src/lib/messages.ts` (thêm khối `feedback`)

**Interfaces:**
- Produces: `type Settled<T> = { ok: true; data: T } | { ok: false; data: null }`;
  `settle<T>(p: Promise<T>): Promise<Settled<T>>`;
  `contentState(x: { failed: boolean; isEmpty: boolean }): 'error' | 'empty' | 'content'`;
  `<LoadErrorState />` (client component, props `{ className?: string }`,
  copy từ `messages.feedback.loadError`, nút retry gọi `router.refresh()`).

- [ ] **Step 1: i18n** — thêm vào `messages.ts` (khối top-level mới, cạnh các
  khối trang):

```ts
  /** Khuôn phản hồi dùng chung của tầng dữ liệu (ADR-0016 §4) — không gắn
      trang cụ thể; trang nào có copy riêng (vd toursPage.loadError) thì dùng
      bản riêng của nó. */
  feedback: {
    loadError: {
      title: "We couldn't load this section",
      body: 'Something went wrong on our side — this is usually momentary.',
      retry: 'Try again',
    },
  },
```

- [ ] **Step 2: Test fail thuần** — `resilience.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { contentState, settle } from './resilience';

describe('settle', () => {
  it('promise resolve → {ok:true, data}', async () => {
    expect(await settle(Promise.resolve([1]))).toEqual({ ok: true, data: [1] });
  });
  it('promise reject → {ok:false, data:null} và KHÔNG throw', async () => {
    expect(await settle(Promise.reject(new Error('down')))).toEqual({ ok: false, data: null });
  });
});

describe('contentState', () => {
  it('failed thắng isEmpty — API sập không được hiện empty-state (nói dối)', () => {
    expect(contentState({ failed: true, isEmpty: true })).toBe('error');
  });
  it('rỗng thật → empty; có dữ liệu → content', () => {
    expect(contentState({ failed: false, isEmpty: true })).toBe('empty');
    expect(contentState({ failed: false, isEmpty: false })).toBe('content');
  });
});
```

- [ ] **Step 3: Chạy fail → viết `resilience.ts`:**

```ts
/**
 * Tri-state của mọi section dữ liệu API (ADR-0016 §4, kế thừa lib/resilience
 * Nexora): lỗi ≠ rỗng ≠ có nội dung. `settle` không bao giờ throw để page
 * SSG/ISR không sập lúc build khi API hắt hơi — lưới an toàn mà ADR-0016 §3
 * dựa vào cho quyết định "build với API sống".
 */
export type Settled<T> = { ok: true; data: T } | { ok: false; data: null };

export async function settle<T>(promise: Promise<T>): Promise<Settled<T>> {
  try {
    return { ok: true, data: await promise };
  } catch {
    return { ok: false, data: null };
  }
}

/** `failed` thắng `isEmpty`: empty-state khi API lỗi là nói dối người dùng. */
export function contentState(input: { failed: boolean; isEmpty: boolean }): 'error' | 'empty' | 'content' {
  if (input.failed) return 'error';
  return input.isEmpty ? 'empty' : 'content';
}
```

- [ ] **Step 4: Test pass.**

- [ ] **Step 5: `LoadErrorState` + spec jsdom.** Component:

```tsx
'use client';

import { messages } from '@tourism/i18n';
import { RotateCwIcon } from 'lucide-react';
import { useRouter } from 'next/navigation';

/**
 * Panel lỗi tải section (ADR-0016 §4). Retry = router.refresh() — chạy lại
 * RSC fetch của trang, đúng mẫu LoadErrorState Nexora. KHÔNG dùng cho lỗi
 * validate form (inline cạnh field) — chỉ cho section dữ liệu API.
 */
export function LoadErrorState({ className = '' }: { className?: string }) {
  const router = useRouter();
  const t = messages.feedback.loadError;
  return (
    <div role="alert" className={`rounded-2xl border border-dashed p-12 text-center ${className}`}>
      <h2 className="font-heading text-xl font-medium text-foreground">{t.title}</h2>
      <p className="mt-2 text-pretty text-muted-foreground">{t.body}</p>
      <button
        type="button"
        onClick={() => router.refresh()}
        className="mt-5 inline-flex cursor-pointer items-center gap-2 text-sm font-medium text-primary hover:underline"
      >
        <RotateCwIcon className="size-3.5" aria-hidden="true" />
        {t.retry}
      </button>
    </div>
  );
}
```

Spec (`load-error-state.spec.tsx`, project `dom`; mock `next/navigation`):

```tsx
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { describe, expect, it, vi } from 'vitest';
import { LoadErrorState } from './load-error-state';

const refresh = vi.fn();
vi.mock('next/navigation', () => ({ useRouter: () => ({ refresh }) }));

describe('LoadErrorState', () => {
  it('có role=alert và nút retry gọi router.refresh', async () => {
    render(<LoadErrorState />);
    expect(screen.getByRole('alert')).toBeInTheDocument();
    await userEvent.click(screen.getByRole('button', { name: /try again/i }));
    expect(refresh).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **Step 6: Chạy toàn bộ test web + i18n** —
`pnpm turbo run test --filter=@tourism/i18n --filter=@tourism/web` → xanh.

- [ ] **Step 7: Commit** — `feat(web): khuôn tri-state settle/contentState + LoadErrorState`

---

### Task 4: Seed 9 bài blog (phía API)

**Files:**
- Create: `apps/api/prisma/fixtures/posts.ts`
- Modify: `apps/api/prisma/seed.ts` (bước 5 mới + sửa header comment)

**Interfaces:**
- Produces: `fixtures/posts.ts` export
  `posts: Array<{ slug; title; excerpt; content; publishedAt; tags: {slug; name}[] }>`
  — KHÔNG có `authorId` (gán lúc runtime bằng admin upsert sẵn có).

- [ ] **Step 1: Viết `fixtures/posts.ts`.** Nguồn: `apps/web/src/mocks/journal.ts`
  (đọc trực tiếp, giữ NGUYÊN VĂN copy đã duyệt). Quy tắc convert mỗi bài:

  1. `sections` → một chuỗi markdown `content`: mỗi section là `## <heading>`,
     tiếp theo các `paragraphs` (mỗi đoạn một dòng trống ngăn cách), `bullets`
     thành dòng `- <bullet>`. Không thêm/bớt chữ.
  2. `slug`/`title`/`excerpt` giữ nguyên.
  3. `publishedAt`: **bẫy đã đo — 4/9 ngày mock ở TƯƠNG LAI** (hôm nay
     31/07/2026) mà `publishedPostWhere()` (ADR-0004) lọc `publishedAt <= now`
     → giữ nguyên là `/blog` chỉ hiện 5 bài. Dời TOÀN BỘ về quá khứ, giữ đúng
     thứ tự, theo bảng (khoá = ngày gốc, duy nhất từng bài):

     | Ngày gốc | publishedAt seed |
     | --- | --- |
     | 2026-10-02 | 2026-07-22T08:00:00Z |
     | 2026-09-18 | 2026-07-08T08:00:00Z |
     | 2026-08-30 | 2026-06-25T08:00:00Z |
     | 2026-08-11 | 2026-06-08T08:00:00Z |
     | 2026-07-21 | 2026-05-20T08:00:00Z |
     | 2026-07-03 | 2026-05-02T08:00:00Z |
     | 2026-06-15 | 2026-04-14T08:00:00Z |
     | 2026-05-29 | 2026-03-27T08:00:00Z |
     | 2026-05-12 | 2026-03-10T08:00:00Z |

  4. `tags`: tag chính = `category` mock (`{ slug: slugify(category),
     name: category }` — slugify thủ công, ví dụ `Packing` → `packing`); cộng
     MỘT tag phụ là địa danh trung tâm của bài nếu tiêu đề/section nêu rõ một
     nơi (ví dụ bài mist season → `{ slug: 'sa-pa', name: 'Sa Pa' }`, bài Hoi
     An food → `{ slug: 'hoi-an', name: 'Hoi An' }`); bài không xoay quanh một
     nơi thì chỉ tag chính. Tên tag là copy user-facing → tiếng Anh.

  Đầu file ghi comment (tiếng Việt) nêu nguồn gốc + bảng dời ngày + lý do
  ADR-0004. Ví dụ hoàn chỉnh phần tử đầu tiên (8 bài còn lại cùng khuôn):

```ts
export const posts = [
  {
    slug: 'what-to-pack-for-the-mist-season',
    title: 'What to pack for the mist season',
    excerpt:
      'A light jacket, real shoes, and patience. The terraces reward all three — our guides share their honest checklist.',
    publishedAt: '2026-07-22T08:00:00Z',
    tags: [
      { slug: 'packing', name: 'Packing' },
      { slug: 'sa-pa', name: 'Sa Pa' },
    ],
    content: [
      '## Layers beat one big coat',
      '',
      'Sa Pa runs cold at 6am and warm by noon. I have watched guests peel off a puffer jacket on the Cat Cat trail before 11am and carry it the rest of the day. A fleece under a thin rain shell handles both ends without the extra weight in your bag.',
      '',
      'Bring gloves for the cable car up to Fansipan. The summit platform sits well below freezing from November through February, and the wind at 3,143 metres cuts through anything thinner than wool.',
      '',
      '## Shoes that already know mud',
      // … các section còn lại đúng quy tắc 1
    ].join('\n'),
  },
  // … 8 bài còn lại
];
```

- [ ] **Step 2: Nối vào `seed.ts`.** Sau bước overlay users (đã có `admin`),
  thêm bước 5 — upsert theo slug (idempotent tự nhiên, chạy lại không nhân bản):

```ts
  // 5. Blog posts — 9 bài port từ mock journal đã duyệt của web (spec
  //    2026-07-31-blog-api-design §2B). Upsert theo slug; tag connectOrCreate
  //    theo slug. Ngày đã dời hết về quá khứ (publishedPostWhere lọc
  //    publishedAt <= now — ADR-0004).
  for (const post of blogPosts) {
    await prisma.post.upsert({
      where: { slug: post.slug },
      create: {
        slug: post.slug,
        title: post.title,
        excerpt: post.excerpt,
        content: post.content,
        status: PostStatus.PUBLISHED,
        publishedAt: new Date(post.publishedAt),
        authorId: admin.id,
        tags: {
          create: post.tags.map((tag) => ({
            tag: { connectOrCreate: { where: { slug: tag.slug }, create: tag } },
          })),
        },
      },
      update: {},
    });
  }
  console.log(`[seed] blog posts: ${blogPosts.length} upserted.`);
```

  Import thêm `PostStatus` từ enums và `posts as blogPosts` từ
  `./fixtures/posts.js`. **Sửa header comment seed.ts**: bỏ `post` khỏi danh
  sách "KHÔNG port", thêm mục 5 vào danh sách "Seed những gì".

- [ ] **Step 3: Chạy seed thật + đo bằng curl** (API + DB Supabase theo
  `.env.local` của `apps/api`):

```bash
pnpm --filter @tourism/api db:seed
```

Expected: log `blog posts: 9 upserted`. Chạy lần 2 → vẫn 9, không lỗi, không
nhân bản. Rồi bật API và đo:

```bash
curl -s http://localhost:3001/api/posts | head -c 600
curl -s http://localhost:3001/api/posts-tags
curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/api/posts/what-to-pack-for-the-mist-season
```

Expected: list có `total: 9`; tags có `packing`, `sa-pa`…; detail → 200.

- [ ] **Step 4: `pnpm gate` (scope API)** —
`pnpm turbo run build typecheck test lint --filter=@tourism/api` → xanh.

- [ ] **Step 5: Commit** — `feat(api): seed 9 bai blog tu mock journal da duyet`

---

### Task 5: `lib/api/posts.ts` — VM + fetch, và đổi chữ ký `lib/blog.ts`

**Files:**
- Create: `apps/web/src/lib/api/posts.ts`
- Test: `apps/web/src/lib/api/posts.spec.ts`
- Modify: `apps/web/src/lib/blog.ts` + `apps/web/src/lib/blog.spec.ts`

**Interfaces:**
- Consumes: `api` (Task 2), `TAGS`/`postTag` (Task 2).
- Produces (component sẽ import type từ đây thay cho `@/mocks/types`):

```ts
/** VM listing — GIỮ TÊN FIELD của MockJournalPost để component đổi tối thiểu. */
export interface JournalPost {
  slug: string;
  title: string;
  excerpt: string;
  date: string;          // YYYY-MM-DD, cắt từ publishedAt
  readMinutes: number;   // dẫn xuất — contract không có
  category: string;      // tên tag ĐẦU TIÊN — chip hiển thị
  author: string;        // author.name ?? messages.blog fallback
  tags: { slug: string; name: string }[];
}
export interface JournalPostDetail extends JournalPost {
  contentMarkdown: string;
}
export function deriveReadMinutes(content: string): number;
export function toJournalPost(dto: PostCard): JournalPost;
export function toJournalPostDetail(dto: PostDetail): JournalPostDetail;
export function fetchPosts(): Promise<JournalPost[]>;           // tag TAGS.POSTS
export function fetchPostTags(): Promise<PostTag[]>;            // tag TAGS.POSTS
export const fetchPostDetail: (slug: string) => Promise<JournalPostDetail | null>;
// ^ bọc React cache(); null khi NOT_FOUND (typed error), throw lỗi khác
```

- [ ] **Step 1: Test fail cho phần thuần** (`posts.spec.ts` — dựng DTO fixture
  tay theo `PostCardSchema`):

```ts
import { describe, expect, it } from 'vitest';
import { deriveReadMinutes, toJournalPost } from './posts';

const dto = {
  id: '0198c9c4-0000-7000-8000-000000000001',
  slug: 'what-to-pack-for-the-mist-season',
  title: 'What to pack for the mist season',
  excerpt: 'A light jacket, real shoes, and patience.',
  publishedAt: '2026-07-22T08:00:00.000Z',
  cover: null,
  tags: [
    { slug: 'packing', name: 'Packing' },
    { slug: 'sa-pa', name: 'Sa Pa' },
  ],
  author: { name: 'Seed Admin', avatarUrl: null },
};

describe('toJournalPost', () => {
  it('map DTO → VM: date cắt YYYY-MM-DD, category = tag đầu, author = name', () => {
    const vm = toJournalPost(dto);
    expect(vm).toMatchObject({
      slug: dto.slug,
      date: '2026-07-22',
      category: 'Packing',
      author: 'Seed Admin',
    });
  });
  it('author.name null → fallback; không tag → category fallback "Journal"; excerpt null → chuỗi rỗng', () => {
    const vm = toJournalPost({ ...dto, author: { name: null, avatarUrl: null }, tags: [], excerpt: null });
    expect(vm.author).toBe('Tourism guides');
    expect(vm.category).toBe('Journal');
    expect(vm.excerpt).toBe('');
  });
});

describe('deriveReadMinutes', () => {
  it('~200 từ/phút, làm tròn lên, tối thiểu 1', () => {
    expect(deriveReadMinutes('one two three')).toBe(1);
    expect(deriveReadMinutes(Array(401).fill('word').join(' '))).toBe(3);
    expect(deriveReadMinutes('')).toBe(1);
  });
});
```

Hai chuỗi fallback (`'Tourism guides'`, `'Journal'`) là copy user-facing →
khai trong `messages.ts` khối `blog: { fallbackAuthor, fallbackCategory }`
và import, KHÔNG hardcode trong lib (luật 7; test trên so giá trị qua
`messages` nếu tiện).

- [ ] **Step 2: Fail → viết `posts.ts`.** Phần fetch:

```ts
import { cache } from 'react';
import { isDefinedError, safe } from '@orpc/client';
import { api } from './client';
import { TAGS, postTag } from './tags';

const REVALIDATE_SEC = 300; // ADR-0016 §3 — con số Nexora đã vận hành

export async function fetchPosts(): Promise<JournalPost[]> {
  // pageSize 50: đủ cho khối lượng hiện tại (9); server-side pagination là nợ
  // có điều kiện kích hoạt ghi ở spec §2C.
  const page = await api.posts.list(
    { page: 1, pageSize: 50, sort: 'publishedAt', order: 'desc' },
    { context: { next: { revalidate: REVALIDATE_SEC, tags: [TAGS.POSTS] } } },
  );
  return page.items.map(toJournalPost);
}

export async function fetchPostTags() {
  return api.posts.tags(undefined, {
    context: { next: { revalidate: REVALIDATE_SEC, tags: [TAGS.POSTS] } },
  });
}

/** React cache(): generateMetadata ↔ thân trang chung MỘT fetch (ADR-0016 §2). */
export const fetchPostDetail = cache(async (slug: string): Promise<JournalPostDetail | null> => {
  const [error, data] = await safe(
    api.posts.bySlug(
      { slug },
      { context: { next: { revalidate: REVALIDATE_SEC, tags: [postTag(slug)] } } },
    ),
  );
  if (error) {
    // POST_NOT_FOUND là nhánh 404 hợp lệ → null cho page gọi notFound().
    if (isDefinedError(error) && error.code === 'POST_NOT_FOUND') return null;
    throw error;
  }
  return toJournalPostDetail(data);
});
```

⚠️ Field khớp `PagedSchema` (`items`/`page`/`pageSize`/`total`/`totalPages` —
mở `libs/shared/contract/src/schemas/common.ts` xác nhận tên field khi viết)
và chữ ký `safe()` của `@orpc/client` 1.14 (đối chiếu context7 như Task 2).

- [ ] **Step 3: Đổi chữ ký `lib/blog.ts`** — thay `MockJournalPost` bằng
  `JournalPost` (import type từ `./api/posts`):
  - Giữ: `sortPostsByDate`, `adjacentPosts`, `relatedPosts` (so theo
    `category` — giờ là tag đầu, ngữ nghĩa giữ), `searchPosts`, `latestPosts`,
    `homeTeaserPosts`, `HOME_TEASER_COUNT`.
  - **Thay** `filterPostsByCategory(posts, category?)` bằng
    `filterPostsByTag(posts, tagSlug?)` — match trên `post.tags.some(t => t.slug === tagSlug)`
    (chip giờ là tag từ `posts.tags`, lọc phải phủ CẢ tag phụ).
  - **Xoá** `postCategories` (nguồn chip giờ là endpoint `posts.tags`).
  - Cập nhật `blog.spec.ts` theo: fixture spec đổi sang shape `JournalPost`
    (thêm `tags`), test `filterPostsByTag` match tag phụ (`sa-pa`) và tag lạ →
    mảng rỗng.

- [ ] **Step 4: Chạy test node project** → pass; typecheck (đỏ ở component còn
  dùng MockJournalPost là ĐÚNG — Task 7/8/9 sửa; nếu muốn giữ typecheck xanh
  từng commit, đổi type import của components sang `JournalPost` ngay trong
  task này, không đổi logic).

- [ ] **Step 5: Commit** — `feat(web): lib/api/posts — VM + fetch ISR/tag, blog.ts sang VM`

---

### Task 6: `tocFromMarkdown` + component `ArticleMarkdown`

**Files:**
- Modify: `apps/web/src/lib/toc.ts` + `apps/web/src/lib/toc.spec.ts`
- Create: `apps/web/src/components/content/article-markdown.tsx`
- Test: `apps/web/src/components/content/article-markdown.spec.tsx`

**Interfaces:**
- Produces: `tocFromMarkdown(md: string): TocItem[]` (chỉ H2 `## `, bỏ heading
  trong code fence, id = `slugify(text)` — PHẢI trùng id `ArticleMarkdown` gắn
  cho `<h2>`); `<ArticleMarkdown markdown={string} />`.

- [ ] **Step 1: Test fail `tocFromMarkdown`** (thêm vào `toc.spec.ts`):

```ts
describe('tocFromMarkdown', () => {
  it('lấy H2 theo thứ tự, id slugify, index pad 2 chữ số', () => {
    const md = '## Layers beat one big coat\n\nbody\n\n## Shoes that already know mud\n';
    expect(tocFromMarkdown(md)).toEqual([
      { id: 'layers-beat-one-big-coat', label: 'Layers beat one big coat', index: '01' },
      { id: 'shoes-that-already-know-mud', label: 'Shoes that already know mud', index: '02' },
    ]);
  });
  it('bỏ qua H3 và heading nằm trong code fence', () => {
    const md = '### not me\n```\n## fenced\n```\n## real one\n';
    expect(tocFromMarkdown(md).map((i) => i.label)).toEqual(['real one']);
  });
});
```

- [ ] **Step 2: Fail → cài đặt** (thuần, tái dùng `slugify` + khuôn `TocItem`):

```ts
/** Mục lục từ markdown: chỉ H2 (`## `) — cùng cấp với section cũ; heading
    trong code fence không phải heading. Id PHẢI khớp id mà ArticleMarkdown
    gắn cho <h2> (cùng slugify) — lệch là TOC trỏ vào không khí. */
export function tocFromMarkdown(markdown: string): TocItem[] {
  const items: TocItem[] = [];
  let inFence = false;
  for (const line of markdown.split('\n')) {
    if (line.trimStart().startsWith('```')) {
      inFence = !inFence;
      continue;
    }
    if (inFence) continue;
    const match = /^##\s+(.+)$/.exec(line);
    if (match?.[1]) {
      items.push({
        id: slugify(match[1]),
        label: match[1].trim(),
        index: String(items.length + 1).padStart(2, '0'),
      });
    }
  }
  return items;
}
```

- [ ] **Step 3: `ArticleMarkdown`:**

```tsx
import Markdown from 'react-markdown';
import remarkGfm from 'remark-gfm';
import { Typeset } from '@tourism/ui/components/typeset';
import { slugify } from '@/lib/slug';

/**
 * Thân bài markdown từ API (PostDetail.content — ADR-0016/spec §2D), render
 * trong Typeset preset reading (ADR-0012). H2 gắn id = slugify(text) để khớp
 * tocFromMarkdown. KHÔNG bật rehype-raw: content là dữ liệu seed của mình
 * nhưng giữ mặc định không-raw-HTML làm lưới (spec §6).
 */
export function ArticleMarkdown({ markdown }: { markdown: string }) {
  return (
    <Typeset preset="reading" className="text-muted-foreground">
      <Markdown
        remarkPlugins={[remarkGfm]}
        components={{
          h2: ({ children }) => <h2 id={slugify(String(children))}>{children}</h2>,
        }}
      >
        {markdown}
      </Markdown>
    </Typeset>
  );
}
```

Spec jsdom: render markdown 2 section → `getByRole('heading', {level: 2})` có
`id` đúng slugify; bullet list render thành `<ul><li>`.

- [ ] **Step 4: Test pass cả node + dom.**
- [ ] **Step 5: Commit** — `feat(web): tocFromMarkdown + ArticleMarkdown (Typeset reading)`

---

### Task 7: Đổi nguồn `/blog` (listing)

**Files:**
- Modify: `apps/web/src/app/(site)/blog/page.tsx` ·
  `apps/web/src/components/blog/blog-explorer.tsx` (+`.spec.tsx`) ·
  `apps/web/src/components/blog/category-chips.tsx` ·
  `apps/web/src/components/blog/post-card.tsx`

**Interfaces:**
- Consumes: `fetchPosts`/`fetchPostTags` (Task 5), `settle`/`contentState`/
  `LoadErrorState` (Task 3), `filterPostsByTag` (Task 5).
- Produces: `BlogExplorer` props `{ posts: JournalPost[]; tags: { slug: string; name: string }[]; initialTag?; initialQuery?; initialPage? }`
  (`initialTag` giờ là **tag slug**).

- [ ] **Step 1: page.tsx** — server fetch + tri-state:

```tsx
export const revalidate = 300; // ADR-0016 §3

export default async function BlogIndexPage({ searchParams }: { searchParams: Promise<{ tag?: string; q?: string; page?: string }> }) {
  const { tag, q, page } = await searchParams;
  const [postsRes, tagsRes] = await Promise.all([settle(fetchPosts()), settle(fetchPostTags())]);
  // Chip là điều hướng phụ — posts sống mà tags chết thì vẫn hiện bài, hàng
  // chip rơi về rỗng; chỉ posts chết mới là lỗi trang.
  const state = contentState({ failed: !postsRes.ok, isEmpty: (postsRes.data ?? []).length === 0 });
  // … ContentHero giữ nguyên …
  {state === 'error' ? (
    <LoadErrorState />
  ) : (
    <BlogExplorer
      posts={postsRes.data ?? []}
      tags={tagsRes.data ?? []}
      initialTag={tag}
      initialQuery={q}
      initialPage={Number(page) || 1}
    />
  )}
}
```

(`state === 'empty'` để BlogExplorer tự xử — nó đã có màn "Nothing here yet".)

- [ ] **Step 2: blog-explorer.tsx** — đổi type import sang `JournalPost`,
  prop `categories: string[]` → `tags: {slug; name}[]`, lọc bằng
  `filterPostsByTag(posts, tag)`; URL vẫn ghi `?tag=<slug>`. `category-chips.tsx`
  nhận `{slug, name}[]`, hiển thị `name`, so sánh active theo `slug`.
  `post-card.tsx` đổi type import (field dùng đều còn nguyên trong VM — không
  đổi markup).

- [ ] **Step 3: Cập nhật `blog-explorer.spec.tsx`** theo props mới (fixture VM
  có `tags`); thêm case: chọn chip tag phụ (`sa-pa`) hiện đúng bài mang tag đó.

- [ ] **Step 4: Chạy test dom + đo tay dev** (API sống): `/blog` hiện 9 bài;
  `?tag=packing` lọc đúng; tắt API → refresh thấy `LoadErrorState` (dev bypass
  cache mỗi request nên đo được ngay).

- [ ] **Step 5: Commit** — `feat(web): /blog doc tu API — tri-state + chip tag`

---

### Task 8: Đổi nguồn `/blog/[slug]` (detail)

**Files:**
- Modify: `apps/web/src/app/(site)/blog/[slug]/page.tsx` ·
  `apps/web/src/components/blog/post-hero.tsx` · `post-nav.tsx`

**Interfaces:**
- Consumes: `fetchPostDetail`/`fetchPosts` (Task 5), `tocFromMarkdown` +
  `ArticleMarkdown` (Task 6), `adjacentPosts`/`relatedPosts` (Task 5).

- [ ] **Step 1: page.tsx:**
  - `export const revalidate = 300;`
  - `generateStaticParams`: `const posts = await fetchPosts(); return posts.map(p => ({slug: p.slug}))`
    — build fetch API sống (ADR-0016 §3); fetch hỏng lúc build → throw → build
    fail TO TIẾNG, đúng chủ đích (đừng settle ở đây: slug rỗng âm thầm là
    sitemap/ISR rỗng âm thầm).
  - `generateMetadata` + body cùng gọi `fetchPostDetail(slug)` (React cache đã
    dedupe); `null` → `notFound()`.
  - `tocFromSections(post.sections)` → `tocFromMarkdown(post.contentMarkdown)`;
    `<ArticleBody sections>` → `<ArticleMarkdown markdown={post.contentMarkdown} />`
    (cụm pháp lý GIỮ ArticleBody — spec §2D).
  - `adjacentPosts`/`relatedPosts`: nguồn `await fetchPosts()` (cùng tag
    TAGS.POSTS — một fetch cache chung với listing).
  - JSON-LD: `datePublished: post.date`, **bỏ `image`** (cover null — không
    bịa URL), **bỏ nhánh `dateModified`** (VM không còn `updated`), author giữ
    `{'@type':'Person', name: post.author}`. Breadcrumb giữ nguyên.
  - `<p className="text-lg">{post.excerpt}</p>`: excerpt VM là `''` khi null →
    bọc điều kiện `post.excerpt ? … : null`.
- [ ] **Step 2: post-hero.tsx / post-nav.tsx** — đổi type import sang
  `JournalPost`; PostHero **xoá nhánh `post.updated`** (spec §2D — cắt có chủ
  đích, contract không có updatedAt).
- [ ] **Step 3: KHÔNG tạo `loading.tsx`** — xác nhận lại bằng
  `find apps/web/src/app -name "loading.tsx"` → rỗng.
- [ ] **Step 4: Đo production build** (API sống, cổng 3000 trống):

```bash
pnpm --filter @tourism/web build && pnpm --filter @tourism/web start &
sleep 3
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/blog/what-to-pack-for-the-mist-season   # 200
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/blog/khong-ton-tai                      # PHẢI 404 thật
```

Xong đo thì kill đúng PID (`ss -ltnp | grep ':3000'`) — cổng sạch.

- [ ] **Step 5: Commit** — `feat(web): /blog/[slug] doc tu API — markdown + toc + 404 that`

---

### Task 9: Teaser Home + rss + sitemap đổi nguồn

**Files:**
- Modify: `apps/web/src/components/home/journal.tsx` ·
  `apps/web/src/app/(site)/page.tsx` (trang Home) ·
  `apps/web/src/app/blog/rss.xml/route.ts` · `apps/web/src/app/sitemap.ts` ·
  `apps/web/src/lib/sitemap.ts` (+`sitemap.spec.ts`)

**Interfaces:**
- Consumes: `fetchPosts` + `settle` + `homeTeaserPosts`.
- Produces: `Journal` (client) nhận props `{ posts: JournalPost[]; failed: boolean }`;
  `sitemapEntries(tours, posts: readonly Pick<JournalPost,'slug'|'date'>[], regions)`.

- [ ] **Step 1: Home.** `journal.tsx` là client component — KHÔNG fetch ở đó.
  Trang Home (server) fetch: `const postsRes = await settle(fetchPosts());`
  truyền `<Journal posts={postsRes.data ?? []} failed={!postsRes.ok} />`.
  Trong `Journal`: `failed` → render `<LoadErrorState className="mt-14" />`
  bên trong section (giữ header section — tri-state, không giấu lỗi thành
  section biến mất); ngược lại giữ nguyên markup, `homeTeaserPosts(posts)`.
  Home vì fetch nên tự thành ISR — **thêm `export const revalidate = 300;`**
  vào `app/(site)/page.tsx` cho tường minh. **Diff Home phải tối thiểu** (spec
  §6): chỉ thêm fetch + 2 props + revalidate, không đổi layout/motion.
- [ ] **Step 2: rss.xml/route.ts** — bỏ `force-static`, thành
  `export const revalidate = 300;` + `export async function GET()`; nguồn
  `settle(fetchPosts())`: fail → `new Response('temporarily unavailable', { status: 503 })`
  (feed sai còn tệ hơn feed vắng); ok → XML như cũ với VM
  (`pubDate` từ `post.date`, `category` = `post.category`).
- [ ] **Step 3: sitemap.** `lib/sitemap.ts`: type tham số posts đổi thành
  `readonly Pick<JournalPost, 'slug' | 'date'>[]`; mục posts thành
  `lastModified: post.date` (VM không còn `updated` — cập nhật comment JSDoc
  cho khớp). `app/sitemap.ts` thành async: posts từ `settle(fetchPosts())`,
  fail → truyền `[]` kèm comment "sitemap thiếu blog TẠM còn hơn build đổ —
  ISR 300s tự chữa"; tours/regions vẫn mock (bước 2/4 sẽ đổi).
  `sitemap.spec.ts`: cập nhật fixture posts (bỏ `updated`), test tổng URL
  38 giữ nguyên con số (9 bài không đổi số lượng).
- [ ] **Step 4: Chạy test node + dom** → xanh.
- [ ] **Step 5: Commit** — `feat(web): teaser Home + rss + sitemap doc tu API`

---

### Task 10: Khai tử mock journal + nghiệm thu toàn cụm

**Files:**
- Delete: `apps/web/src/mocks/journal.ts`
- Modify: `apps/web/src/mocks/types.ts` (xoá `MockJournalPost`) ·
  `apps/web/src/mocks/mocks.spec.ts` (xoá case journal)

- [ ] **Step 1: Xoá mock.** `grep -rn "mocks/journal\|MockJournalPost" apps/web/src`
  → phải RỖNG sau khi xoá (còn hit nào là Task 5–9 sót consumer). Xoá file +
  type + case spec liên quan.
- [ ] **Step 2: `pnpm gate` đầy đủ** (API sống, cổng 3000 trống trước khi
  build) → 18/18 task xanh. `pnpm test:int` không chạy được ở máy này — ghi nợ
  CI vào CHANGELOG entry lúc merge.
- [ ] **Step 3: Nghiệm thu spec §5** trên production build (`next start`,
  API sống) — checklist:
  1. `/blog` đúng 9 bài; chip tag lọc; search "bun cha" ra bài bún chả.
  2. `curl` slug lạ → **404**; 3 slug thật → 200.
  3. Detail: markdown render trong Typeset, TOC trỏ đúng anchor, adjacent +
     related + JSON-LD (không field `image`, không `dateModified`).
  4. Home teaser 3 bài mới nhất (`2026-07-22` đứng đầu).
  5. `rss.xml` 200, 9 item; `sitemap.xml` giữ 38 URL.
  6. Tri-state: tắt API, dev server, `/blog` → `LoadErrorState` (không phải
     "Nothing here yet").
- [ ] **Step 4: Kill mọi tiến trình tự mở, báo "cổng sạch". Commit cuối** —
  `test(web): nghiem thu cum blog API` (nếu có sửa test vụn) — rồi DỪNG:
  **user review + quyết merge** (không tự merge/push). Sau merge: docs sweep
  theo luật 13 (CHANGELOG entry — nhớ luật dấu `+` đầu dòng, cập nhật
  docs/README.md hàng spec bước 1 thành ✅, cập nhật trạng thái P3b).
