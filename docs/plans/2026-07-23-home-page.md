# Trang Home tĩnh + shell chung — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dựng trang Home tĩnh (mock) + shell chung theo spec `2026-07-23-home-page-design.md` — vòng review 1 cho user.

**Architecture:** RSC-first; motion chỉ trong client island (`Reveal`, `AnimatedCounter`, `SearchCard`, hero intro, mobile menu). Mock tập trung có type. Section = file riêng, page.tsx chỉ compose.

**Tech Stack:** Next 16 App Router · @tourism/ui (shadcn base-nova) · tokens Wuling · `motion` (framer-motion v12, import `motion/react`) · next/image.

Spec: [2026-07-23-home-page-design.md](../specs/2026-07-23-home-page-design.md)

## Global Constraints

- CHỈ Home + shell — không tạo route nào khác (nav link các trang chưa có trỏ `#`).
- Copy user-facing tiếng Anh (#7) · comment tiếng Việt (#8) · màu 100% semantic token, KHÔNG hex trong component (#6) · KHÔNG tham chiếu `--region-*` trong @tourism/ui (chỉ page-level web — ADR-0013 #4; regions-strip là page-level nên ĐƯỢC).
- Tuân luật skill shadcn: FieldGroup/Field cho form, `gap-*` không `space-y-*`, `size-*`, icon `data-icon`, không tự chế button/badge bằng div.
- Mọi animation qua `MotionConfig reducedMotion="user"` hoặc `useReducedMotion`.
- Không merge/push — trang chờ user chốt qua nhiều vòng review. `pnpm gate:int` trước khi báo cáo.
- Nhánh: `feat/home-page` (đã tạo, spec đã commit `784e93e`).

---

### Task 1: Dep `motion` + ảnh mock + vitest cho apps/web

**Files:**
- Modify: `apps/web/package.json` (dep `motion`, devDep `vitest@4.1.10`, script `"test": "vitest run"`)
- Create: `apps/web/vitest.config.ts` · `apps/web/public/mock/*.jpg` (7 ảnh) · `apps/web/public/mock/CREDITS.md`

**Interfaces:**
- Produces: ảnh `/mock/{halong,sapa,hoian,hue,mekong,danang,journal-mist}.jpg` (~1600px, nén sharp); vitest chạy được trong apps/web (alias `@/` → `./src`, environment node).

- [ ] **Step 1**: thêm dep + `pnpm install`. vitest.config.ts giống libs/shared/ui (alias `@/` → `src`, `@tourism/ui` → `../../libs/shared/ui/src`, esbuild jsx automatic, env node).
- [ ] **Step 2**: tải 7 ảnh Unsplash chủ đề VN (curl `images.unsplash.com/photo-<id>?w=1600&q=80`, chọn ID lúc chạy bằng cách thử vài ID đã biết chủ đề; ảnh nào 404 thì thay ID khác), nén qua sharp nếu >400KB, ghi nguồn vào CREDITS.md.
- [ ] **Step 3**: verify `ls -la public/mock` (7 jpg, mỗi file 100–400KB) → commit `feat(web): dep motion + ảnh mock + vitest`.

### Task 2: Mock data có type (TDD)

**Files:**
- Create: `apps/web/src/mocks/types.ts`, `tours.ts`, `regions.ts`, `testimonials.ts`, `journal.ts`
- Test: `apps/web/src/mocks/mocks.spec.ts` (viết TRƯỚC)

**Interfaces (Produces — mọi task sau dùng đúng shape này):**

```ts
// types.ts — shape TỰ DO theo nhu cầu UI (công cụ khám phá schema, không ép Prisma)
export type MockRegionKey = 'north' | 'central' | 'south';
export interface MockTour {
  slug: string; title: string; region: MockRegionKey;
  place: string;            // "Quang Ninh · junk boat"
  days: number; priceUsd: number; compareUsd?: number;
  rating: number; reviews: number;
  tags: string[];           // ["Nature","Easy"]
  image: string;            // "/mock/halong.jpg"
  flag?: string;            // "−20% TODAY" — đỏ sơn mài, tối đa 1 tour có flag
  summary: string;
}
export interface MockRegion { key: MockRegionKey; name: string; tagline: string; tourCount: number; }
export interface MockTestimonial { name: string; tour: string; quote: string; rating: number; }
export interface MockJournalPost { slug: string; title: string; excerpt: string; date: string; readMinutes: number; image: string; }
```

Data: 6 tour (Ha Long Bay Cruise — 2 days/north · Sa Pa Terraces Trek — 3 days/north · Hoi An Lantern Evening/central · Hue Imperial Day/central · Mekong Delta Boats/south · Da Nang Coast Ride/central), 3 region (North/Central/South Vietnam + tagline), 3 testimonial, 3 journal post. Export mảng `TOURS`, `REGIONS`, `TESTIMONIALS`, `JOURNAL_POSTS`.

- [ ] **Step 1** (test FAIL trước) — `mocks.spec.ts`: 6 tour; slug duy nhất; `image` bắt đầu `/mock/` và file tồn tại trong `public` (fs.existsSync); rating ∈ [0,5]; đủ 3 region key ở REGIONS và mỗi key xuất hiện ≥1 lần trong TOURS; tối đa 1 tour có `flag`.
- [ ] **Step 2**: chạy fail → viết types + data → chạy PASS (`pnpm turbo run test --filter=@tourism/web`).
- [ ] **Step 3**: commit `feat(web): mock data trang Home (types tự do khám phá schema)`.

### Task 3: Shell — Logo + SiteHeader + SiteFooter, wire vào layout

**Files:**
- Create: `apps/web/src/components/logo.tsx`, `site-header.tsx`, `site-footer.tsx`
- Modify: `apps/web/src/app/layout.tsx` (bọc `<SiteHeader/>{children}<SiteFooter/>`)

**Interfaces:**
- Produces: `<Logo />` (mark + wordmark, prop `className?`), `<SiteHeader />`, `<SiteFooter />` — RSC trừ mobile menu island bên trong header.

Logo — mark Vectory 2 path (bỏ clipPath thừa của bản gốc), lá = primary, chấm = foreground, wordmark Literata:

```tsx
// Mark: nguồn PrebuiltUI free placeholder logos (Vectory) — chỉ dùng phần mark,
// wordmark là chữ của mình. Lá nhuộm primary, chấm giữ foreground theo theme.
export function Logo({ className }: { className?: string }) {
  return (
    <span className={cn('flex items-center gap-2', className)}>
      <svg viewBox="0 0 63 77" aria-hidden="true" className="h-7 w-auto">
        <path
          className="fill-primary"
          d="M33.817 52.382c0-15.988 12.96-28.948 28.948-28.948v17.585c0 15.987-12.96 28.948-28.948 28.948zm-4.869 0c0-15.988-12.96-28.948-28.948-28.948v17.585c0 15.987 12.96 28.948 28.948 28.948z"
        />
        <path
          className="fill-foreground"
          d="M31.487 0c0 8.764 7.049 15.881 15.786 15.992l.207.001-.207.001c-8.737.11-15.786 7.228-15.786 15.992 0-8.833-7.16-15.993-15.993-15.993 8.833 0 15.993-7.16 15.993-15.993"
        />
      </svg>
      <span className="font-heading text-xl font-semibold tracking-tight">
        tour<span className="text-primary">ism</span>
      </span>
    </span>
  );
}
```

Header: `sticky top-0 z-sticky` (var z), `bg-background/85 backdrop-blur-md border-b`, container `max-w-content`; desktop nav 4 link `text-muted-foreground hover:text-foreground` (href `#` các trang chưa có), CTA `<Button>Book a tour</Button>`; mobile: island `mobile-menu.tsx` ("use client", `Sheet` + `Menu` icon button `variant="ghost"`).
Footer: `bg-[nền tối]` — dùng class `dark` trick? KHÔNG — footer tối cả 2 theme: bọc `<footer className="dark bg-background text-foreground">` (re-use dark tokens qua class `dark`, không hex — hợp lệ vì `.dark` đặt lại var). 3 cột: Logo + tagline · links Tours/Journal/Contact/Terms (`#`) · newsletter mini (Field + InputGroup + Button, no-op) + dòng credit ảnh Unsplash.

- [ ] **Step 1**: viết 4 file, build + typecheck xanh.
- [ ] **Step 2**: screenshot `/` (placeholder giữa header/footer) soát mắt.
- [ ] **Step 3**: commit `feat(web): shell chung — logo mark + header + footer`.

### Task 4: Motion islands

**Files:**
- Create: `apps/web/src/components/motion/reveal.tsx`, `animated-counter.tsx`

**Interfaces (Produces):**
- `<Reveal delay?={s} y?={px} className?>` — client, `motion.div` `initial={{opacity:0, y}}` → `whileInView` (`once: true`, margin `-80px`), spring `stiffness 260 damping 60`; bọc `MotionConfig reducedMotion="user"` tại chỗ dùng gốc (đặt MotionConfig một lần trong hero + từng section dùng Reveal đã tự respect qua context? — ĐẶT `MotionConfig` MỘT LẦN trong `reveal.tsx` export thêm `MotionProvider` bọc ở page.tsx).
- `<AnimatedCounter to={number} decimals?={0} suffix?="" />` — client, `useInView` + `animate()` từ `motion`, render `tabular-nums`.

```tsx
// reveal.tsx (rút gọn — code thật kèm comment tiếng Việt)
'use client';
import { MotionConfig, motion } from 'motion/react';
export function MotionProvider({ children }: { children: React.ReactNode }) {
  return <MotionConfig reducedMotion="user">{children}</MotionConfig>;
}
export function Reveal({ children, className, delay = 0, y = 28 }: RevealProps) {
  return (
    <motion.div
      className={className}
      initial={{ opacity: 0, y }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true, margin: '-80px' }}
      transition={{ type: 'spring', stiffness: 260, damping: 60, delay }}
    >
      {children}
    </motion.div>
  );
}
```

- [ ] **Step 1**: viết 2 file, typecheck. Commit `feat(web): motion islands Reveal + AnimatedCounter`.

### Task 5: 8 section + TourCard + SearchCard + page.tsx

**Files:**
- Create: `apps/web/src/components/home/{hero,search-card,tour-card,featured-tours,regions-strip,why-us,stats,testimonials,journal-preview,cta-band}.tsx`
- Modify: `apps/web/src/app/page.tsx`

**Interfaces:**
- Consumes: mocks Task 2, shell Task 3, islands Task 4, `@tourism/ui` (Button, Card*, Badge, Field*, InputGroup*, Separator, Avatar*), lucide icons.
- Produces: `<TourCard tour={MockTour} />` — chuẩn tái dùng cho listing/vùng sau này.

Chỉ dẫn cô đọng từng section (compose, không tự chế primitive):

1. **hero.tsx** (`'use client'` — stagger load): nền = 3 lớp div gradient token (`from-primary/…` KHÔNG đủ — panorama dùng inline style var: `linear-gradient(178deg, var(--chart-2)…)`? KHÔNG — dùng đúng công thức demo đã duyệt nhưng qua token: lớp scene `bg-[linear-gradient(178deg,var(--color-jade-sky,#0000)_0%,…)]` phức tạp → GIẢI PHÁP: thêm util class `.hero-scene` vào `apps/web/src/app/globals.css` (file app, được phép CSS thuần) dùng `var(--primary)`/`var(--foreground)`/`color-mix` — không hex). Nội dung: eyebrow uppercase tracking · h1 `font-heading` clamp · sub · `<SearchCard/>` · trust meta (★ `text-rating`). Stagger `motion.div` variants.
2. **search-card.tsx** (`'use client'`): Card `bg-card/95 backdrop-blur` + `FieldGroup` ngang (3 `Field`: Where to/Dates/Travelers — `InputGroup` + icon `MapPin/Calendar/Users` `data-icon`) + `<Button size="lg">Search tours</Button>` no-op `onSubmit preventDefault`.
3. **tour-card.tsx** (RSC): `Card` overflow-hidden p-0; ảnh `next/image` `aspect-[var(--aspect-card)]` + `sizes`; `flag` → `Badge variant="destructive"` absolute; body: title `font-heading`, place `text-muted-foreground text-sm`, tags → `Badge variant="secondary"`, ★ `text-rating` + reviews, giá `font-semibold tabular-nums` + `compareUsd` line-through `text-price-compare`... (giá format `Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0})` inline — không chế helper). Footer: `<Button size="sm">Book now</Button>` + ghost Wishlist (`Heart`).
4. **featured-tours.tsx** (RSC): section head (eyebrow + h2 + link "View all 68 tours →" href `#`) + grid `sm:grid-cols-2 lg:grid-cols-3 gap-6`, mỗi card bọc `Reveal delay={i*0.06}`.
5. **regions-strip.tsx** (RSC): 3 card `data-region={r.key}`; mỗi card nền `style={{background:'linear-gradient(160deg, var(--region-surface), var(--region-primary))'}}` (page-level được phép var region), chữ `var(--region-on-surface)` phần nhạt / trắng phần đậm, tên + tagline + `tourCount` + mũi tên. Hover lift (`Reveal` + css `transition`).
6. **why-us.tsx** (RSC): nền `bg-muted` full-bleed; 4 item icon (`Compass`,`Users`,`ShieldCheck`,`BadgeDollarSign`) trong vòng `bg-card` + title + mô tả ngắn; grid 2×2 → 4 cột lg.
7. **stats.tsx** (RSC bọc island): 4 `AnimatedCounter` (68 Tours · 12400 Travelers `+` · 140 Local guides `+` · 4.9 `★` decimals 1 — ★ màu `text-rating`).
8. **testimonials.tsx** (RSC): 3 Card quote — `Typeset`? KHÔNG (quote ngắn, không phải content render) — blockquote thường `font-heading italic` + ★ + `Avatar` fallback chữ cái + tên + tour.
9. **journal-preview.tsx** (RSC): 3 card ảnh + date/readMinutes `text-muted-foreground text-xs` + title `font-heading` + excerpt; link "Read the journal →" `#`.
10. **cta-band.tsx** (RSC + form island nhỏ? no-op → để form thường `action="#"`): full-bleed `bg-primary` gradient sang `--chart-1`? — dùng `.cta-band` class trong globals app css với `color-mix(in oklab, var(--primary), var(--foreground) 25%)`; nội dung: eyebrow + h2 trắng + input email (InputGroup nền sáng) + `<Button variant="secondary">Send my routes</Button>`.
11. **page.tsx**: `<MotionProvider>` bọc tất cả; thứ tự section như spec; các dải full-bleed tự quản nền, còn lại trong `container max-w-content px-6`.

- [ ] **Step 1**: viết 10 file + page.tsx; build + typecheck + biome xanh từng cụm.
- [ ] **Step 2**: commit `feat(web): trang Home tĩnh — 8 section + TourCard + SearchCard`.

### Task 6: Verify + gate + báo cáo vòng 1

- [ ] **Step 1**: dev server → screenshot full-page: light 1280 · dark 1280 (gắn class sau hydration — script scratchpad sẵn) · mobile 390 light. Tự soát: contrast, khoảng thở, motion không che nội dung khi reduced-motion.
- [ ] **Step 2**: `pnpm gate:int` xanh (mock spec + toàn suite).
- [ ] **Step 3**: báo cáo user kèm screenshot — KHÔNG merge; chờ vòng chỉnh.

## Self-review

- Spec coverage: 8 section ✓ shell ✓ mock+type ✓ motion đậm + reduced-motion ✓ logo ✓ ảnh tải về ✓ vitest ✓ không trang mới ✓.
- Placeholder: không còn TBD; các chỉ dẫn section đủ cụ thể để không phải quyết thêm.
- Type consistency: `MockTour.image` ↔ test exists ↔ TourCard next/image; `MockRegionKey` ↔ `data-region` ↔ REGIONS tokens.
