# Tài liệu — tourism-v2

Đây là **cửa vào duy nhất**. Mọi tài liệu của dự án đều nằm dưới `docs/` và
được liệt kê ở đây — không có spec nào nằm chỗ khác.

## Sáu thể loại, đừng lẫn

| Thư mục | Trả lời câu hỏi | Viết khi nào |
| --- | --- | --- |
| [`adr/`](adr/) | **Vì sao** chọn thế này? | TRƯỚC khi code (luật CLAUDE.md #5) |
| [`specs/`](specs/) | **Sẽ xây gì** ở phase này? | Đầu mỗi phase, user duyệt rồi mới code |
| [`plans/`](plans/) | **Làm theo bước nào** để hiện thực spec? | Sau khi spec được duyệt, trước khi code |
| [`analysis/`](analysis/) | **Học được gì** từ Nexora? | Khi cần dữ liệu để ra quyết định |
| [`conventions/`](conventions/) | Quy tắc **áp dụng mãi mãi** | Khi một bài học cần thành luật |
| [`design/`](design/) | Đồ nghề **Claude Design** (brief ngôn ngữ thiết kế · prompt từng luồng · [`mockups/`](design/mockups/) nguồn HTML của bản demo trả về · [`references.md`](design/references.md) mẫu đã khảo sát) | Khi cần thiết kế trang mới ngoài code; brief là BẢN TRÍCH — code thắng, sửa brief theo code (commit chính thức 04/08); mockup là ẢNH CHỤP một vòng thiết kế, cách dựng lại ghi ở header `build.mjs` (04/08); references khảo sát 06/08 cho khu account + booking |

Ngoài ra: [`CHANGELOG.md`](CHANGELOG.md) — lịch sử mỗi merge (phase đang
chạy; entry cũ hơn 30/07 ở [`changelog/`](changelog/) — 2 archive theo kỷ
nguyên, di chuyển nguyên văn 03/08) ·
[`skills.md`](skills.md) — skill đã cài & dùng khi nào ·
[`../CLAUDE.md`](../CLAUDE.md) — hợp đồng vận hành.

## ADR — quyết định kiến trúc

| # | Nội dung |
| --- | --- |
| [0001](adr/0001-tech-stack.md) | Tech stack + lộ trình vertical slice + chiến lược UI |
| [0002](adr/0002-payment-gateway-refund-ledger.md) | PaymentGateway interface · Refund ledger · atomic claim thế hệ 2 |
| [0003](adr/0003-auth-fail-closed.md) | Auth mặc định fail-closed — global guard + `@Public()` |
| [0004](adr/0004-post-visibility-helper.md) | Post visibility — helper bắt buộc `publishedPostWhere()` (status + publishedAt<=now) |
| [0005](adr/0005-media-read-build-url.md) | Media đọc — API dựng & trả Cloudinary URL (chỉ cần cloud name công khai) |
| [0006](adr/0006-pending-lifecycle.md) | **Accepted** — vòng đời PENDING: CHECKOUT_FAILED+re-checkout · expired→CANCELLED · cron sweep · self-cancel (BK-1/BK-2/PAY-1/WRK-1) |
| [0008](adr/0008-admin-bootstrap-verified.md) | Admin bootstrap — promote gated `emailVerified` + reconcile lúc boot (SEC-1/AUTH-1/AUTH-2) |
| [0009](adr/0009-refund-correctness.md) | Đúng đắn refund — advisory-lock serialize + trigger `SUM≤total` + gate re-derive orphan (BK-R1/PAY-R1/TOCTOU) |
| [0010](adr/0010-infra-hardening.md) | Infra hardening trước P3b — global exception filter (envelope oRPC) + `@fastify/helmet` + Sentry env-gated |
| [0011](adr/0011-p3b-web-architecture.md) | Kiến trúc web P3b — Next.js 16 + React 19 + Tailwind v4; shared UI `libs/shared/ui` (web+admin), custom-in-app; Biome-không-ESLint |
| [0012](adr/0012-typeset-typography.md) | Typography nội dung render — vendor shadcn/typeset (lõi nguyên bản + 3 preset docs/chat/reading) trong `@tourism/ui`, thay `@tailwindcss/typography` của Nexora |
| [0013](adr/0013-wuling-theme-tokens.md) | Theme Wuling + region tint — giữ pipeline Style Dictionary P0, thay giá trị brand, lớp `--region-*` (north/central/south), wire ui vào tokens.css, font Be Vietnam Pro + Lora. ⚠️ **Bị [ADR-0015](adr/0015-retire-region-tint.md) đảo một phần** (lớp `--region-*` override đã rút toàn site) |
| [0014](adr/0014-web-component-testing.md) | Test tầng component `apps/web` — Vitest 2 project (node + jsdom) + Testing Library, ranh giới với test logic thuần, ghim 1 bản React cho workspace |
| [0015](adr/0015-retire-region-tint.md) | Rút lớp tint theo vùng TOÀN SITE — xoá 3 khối `[data-region]`; trang vùng, Home gallery và About timeline/gallery dùng thẳng token brand; hero về `bg-hero` + TopoPattern; giữ `regionDefaults` cho 4 file auth/contact; bản sắc vùng chuyển sang cấu trúc |
| [0016](adr/0016-web-data-layer.md) | Tầng dữ liệu web — oRPC `OpenAPILink` từ contract (không codegen, không fetch trần) · server-first + ranh giới SC/CC (enquiry/newsletter browser-direct vì throttle per-IP) · SSG→ISR 300s + cache-tag từ ngày đầu, KHÔNG `cacheComponents`; on-demand revalidation là bước riêng sau bước nối 1–4 · khuôn `settle()`/`LoadErrorState`, cấm empty-state khi lỗi, `loading.tsx` mặc định KHÔNG · mock chết dần theo trang · session Better Auth chốt ở bước 7 |
| [0017](adr/0017-web-session-better-auth.md) | Session Better Auth ở web — cookie httpOnly thẳng browser↔API (không proxy/Bearer; API là auth server nên không có lớp sync-user của Nexora) · `useSession` client island, cấm đọc session ở layout public · `proxy.ts` matcher hẹp + defense-in-depth cho bước 8–10 · prod PHẢI cùng registrable domain (crossSubDomainCookies; `sameSite:none` là fallback) · verify email chuyển OTP (plugin emailOTP, hook promote admin phải sống) · 2FA PARK |
| [0018](adr/0018-web-map-library.md) | Bản đồ web `/contact` — `maplibre-gl` pin chính xác `5.24.0` + tile OpenFreeMap positron/dark (không CARTO — cần licence thương mại), viết riêng `contact-map.tsx` trong `apps/web` (không port cả primitive `map.tsx` của mapcn vào `@tourism/ui`), ngoại lệ luật token cho URL style bên ngoài, tiền lệ đầu tiên `vi.mock` một thư viện render nặng (phạm vi hẹp đúng module `contact-map`, không mock cả `@tourism/ui` như Nexora buộc phải làm) |
| [0019](adr/0019-color-token-roles.md) | Tách vai token màu — `primary` đang gánh BA vai loại trừ nhau ở dark (bề mặt cần tối để cõng nhãn gần-trắng `L ≤ 0.542`; chữ cần sáng để đọc trên `muted` `L ≥ 0.74`; vòng focus cần sáng để nhìn thấy). Không giá trị L nào thoả cả ba — lịch sử `cf8f821` (hạ primary cứu nhãn) và `121cff6` (nâng nền cứu chữ thường) là chuỗi vá chạy vòng quanh đúng mâu thuẫn này. Quyết định: `primary` giữ một vai bề mặt · thêm token vai chữ (dark `L 0.76`) · `ring`/`sidebar-ring` NÂNG lên 0.60 (ngược hướng với primary) · nút primary dark lấy ranh giới 3:1 từ VIỀN chứ không từ nền (WCAG 1.4.11 chỉ đòi ranh giới) · `border` và `input` KHÔNG cùng luật — `input` là ranh giới điều khiển phải đạt 3:1, `border` là trang trí, nâng lên sẽ phá ngôn ngữ hairline. Sổ nợ A3 ghi SAI ngưỡng (2.91/2.57 là cặp bề mặt 3:1, không phải chữ 4.5:1) và đếm thiếu — thật ra 9 cặp trượt, tệ nhất 2.05 |
| [0020](adr/0020-real-images-sourcing.md) | ⚠️ **mục 1 AMENDED 08/08 — lô ảnh đầu bị từ chối toàn bộ.** Ảnh thật thay `ImagePlaceholder`. Nguồn: Wikimedia Commons chính (lọc theo toạ độ GPS thật), Pixabay lấp chỗ; **loại đường API Unsplash** vì API Terms bắt hotlink, mâu thuẫn kiến trúc rehost (lưu ý Unsplash *License* cho ảnh tải từ web thì vẫn hợp lệ). Ghi công là ĐIỀU KIỆN giấy phép (chỉ 8–10% Commons là CC0/PD) → thêm 4 cột `author`/`license`/`licenseUrl`/`sourceUrl`. ShareAlike giải bằng kỹ thuật: transform CHỈ `f_auto,q_auto,w_`, **cấm `c_fill`/`c_crop`** vì cắt cúp tạo tác phẩm phái sinh còn đổi định dạng và thu nhỏ theo tỉ lệ thì không. Kho ảnh theo ĐỊA DANH nuôi gallery TOUR. Ảnh người thật KHÔNG seed. **Chỗ SAI ở mục 1:** geosearch kiểm chứng VỊ TRÍ chứ không kiểm chứng CHỦ THỂ — ảnh bãi rác chụp tại Hạ Long qua sạch mọi bộ lọc; 189 ảnh đã seed rồi bị xoá hết, commit script chọn ảnh đã bỏ khỏi nhánh. Lần sau bắt buộc: cửa lọc theo chủ thể/chất lượng (ứng viên: category thẩm định `Quality images`/`Featured pictures` của Commons), và duyệt mắt phải đứng TRƯỚC upload. **✅ THI HÀNH 14/08** (CHANGELOG cùng ngày): cửa lọc chủ thể giải bằng **người chọn — user gửi link ảnh**, máy chỉ khuân; duyệt-trước-upload dựng thành ranh giới giữa hai script (`media:scan` CHỈ báo cáo, `media:upload` đọc `--json` của nó). Cây thả ảnh `media-inbox/` (gitignored) + 4 lệnh `media:tree`/`fetch`/`scan`/`upload` ở `apps/api/scripts/`. §7 (ảnh người thật) đã dùng thật để LOẠI một tấm: ảnh có mặt người nhận diện được không được gắn vào caption khai đó là ảnh của một khách hàng cụ thể |
| [0021](adr/0021-media-write-surface.md) | Media GHI — signed direct upload Cloudinary cho khách (browser POST thẳng, API chỉ ký; publicId server sinh, folder theo purpose) · `AVATAR` mọi user authed / `REVIEW_PHOTO` gate theo cùng luật eligibility của reviews.create · avatar ghi qua `account.setAvatar` đóng (cấm client set `updateUser.image` tự do) · ảnh review tái dùng `MediaAsset` + enum `REVIEW`, moderation ăn theo `isApproved` · thiếu cặp key API vẫn boot (signUpload 503) · rác Cloudinary ghi sổ P4 (cron reconcile) |
| [0022](adr/0022-tour-detail-tabs.md) | Tour Details bỏ cuộn-dài-có-mục-lục, chuyển sang **5 tab** (Overview · Itinerary · Departures · Reviews · Good to know) theo mẫu ReUI `product-detail-1` · phải render ĐỦ 5 panel rồi ẩn bằng CSS (trang SSG nằm trong sitemap, mount có điều kiện là giấu lịch trình khỏi crawler) · tab đồng bộ hash URL, `OnThisPage` rời trang này · trạng thái live của timeline chỉ hiện cho session có booking PAID đúng đợt · nút Write a review biến mất (POST /api/reviews đòi bookingCode mà trang tour không có) |
| [0023](adr/0023-tour-merchandising-fields.md) | Năm cột nội dung bán hàng cho `Tour` — bốn `fact*Note` varchar(280) cho card dữ kiện (cột RỜI, không JSON: bốn ô cố định của một bố cục đã chốt) + `freeCancellationDays Int?` **không parse từ `policy.body`** (đếm được: regex "up to N days" chỉ bắt 12/29 câu, và 15/30 tour ghi cửa sổ bằng GIỜ nên để null) · tiêu đề thẻ policy KHÔNG cần cột mới, sửa ở tầng fixture · hai bước seed đổi sang `upsert` vì `skipDuplicates` không bao giờ đưa nội dung sửa tới DB đang chạy |

## Specs — theo phase

| Phase | Spec | Trạng thái |
| --- | --- | --- |
| P1 API lõi | [2026-07-18-p1-api-core](specs/2026-07-18-p1-api-core.md) | ✅ đã merge |
| P2 Money-path | [2026-07-18-p2-money-path](specs/2026-07-18-p2-money-path.md) | ✅ đã merge |
| P3a API khách hàng | [2026-07-19-p3a-customer-api](specs/2026-07-19-p3a-customer-api.md) | ✅ đã merge (A+B+C). ⚠️ **Bất biến rating của gate ③ đã ĐẢO 31/07**: spec/code lúc merge lọc `source === VERIFIED` (loại CURATED) khi recompute `Tour.ratingAvg`; quyết định 31/07 đảo lại — mọi review đã duyệt có `tourId` (kể cả CURATED) đều tính, vì capstone không có khách thật nên CURATED là nguồn sao duy nhất. Đọc doc-comment `moderate()` trong `apps/api/src/modules/reviews/reviews.service.ts`, đừng đọc spec này như hiện trạng cho phần này |
| P3a closeout (C1·R1·R2) | [2026-07-21-p3a-contract-closeout-design](specs/2026-07-21-p3a-contract-closeout-design.md) | ✅ đã merge |
| Typeset `@tourism/ui` | [2026-07-22-ui-typeset-design](specs/2026-07-22-ui-typeset-design.md) | ✅ đã merge — nền của [ADR-0012](adr/0012-typeset-typography.md). *Thiếu khỏi bản đồ từ 22/07 tới 30/07 — thêm vào ở đợt rà soát docs* |
| Theme Wuling + region tokens | [2026-07-22-wuling-theme-tokens-design](specs/2026-07-22-wuling-theme-tokens-design.md) | ✅ đã merge — nền của [ADR-0013](adr/0013-wuling-theme-tokens.md). ⚠️ **Phần lớp `--region-*` trong spec này đã bị [ADR-0015](adr/0015-retire-region-tint.md) rút bỏ (30/07)** — đọc ADR-0015 trước, đừng đọc spec như hiện trạng. *Cũng thiếu khỏi bản đồ tới 30/07* |
| P3b Web | [ADR-0011](adr/0011-p3b-web-architecture.md) | 🚧 Home ✅ + navbar ✅ + /about ✅ + /contact ✅ + auth 6/6 ✅ + gia vị bản đồ ✅ + cụm pháp lý/utility ✅ 25/07 + **cụm Blog ✅ merge 27/07** (/blog · /blog/[slug] · rss.xml) + thân trang 404 & nền lưới động Contact ✅ 27/07 + **cụm Tours ✅ merge 27/07** (`/tours` · `/tours/[slug]`) + **robots.txt/sitemap.xml (tours+blog) ✅ 28/07** + **hình ảnh & uy tín trang chi tiết ✅ merge 28/07** (card thiết kế lại 5 vòng · gallery khảm + lightbox · khu Traveller reviews); **cụm Destinations ✅ merge 30/07** (`/destinations` · `/destinations/[region]` ×3 · ADR-0015 rút tint vùng toàn site · chuyển động 3 trục miền); ****chính sách ảnh — ĐỔI 14/08**: không còn "toàn site `ImagePlaceholder`" nữa. Quy trình ảnh thật đã chạy (cây `media-inbox/` + `media:tree`/`fetch`/`scan`/`upload`, xem CHANGELOG 14/08); user gửi link, script khuân, duyệt mắt đứng TRƯỚC upload. Bề mặt CHƯA có ảnh vẫn giữ `ImagePlaceholder` và đó là trạng thái BÌNH THƯỜNG, không phải lỗi — `SlotImage` tự rơi về giữ chỗ khi khe rỗng**; **bước 1 nối API ✅ merge 31/07** (cụm Blog + nền `lib/api` — trang đầu tiên rời mock, SSG→ISR 300s + cache-tag); **bước 2+3 ✅ merge 31/07** (catalogue THẬT 30 tour thay trọn seed + `/tours` + detail + sitemap 52 URL; bất biến rating đảo — xem CHANGELOG); **bước 4 ✅ merge 01/08** (Destinations + Home + /about thuần API — 24/24 link tour 200, mocks catalogue khai tử; `mocks/regions.ts` **vẫn còn, CỐ Ý** — khung 3 miền (key/slug/name) không có endpoint tương ứng, xem [analysis 30/07](analysis/2026-07-30-docs-audit-progress.md); navbar giữ 4-link — thiết kế lại 30/07, tái xác nhận 01/08; docs sweep 03/08); **bước 5+6 ✅ merge 03/08** (form contact + newsletter + trang unsubscribe — hành vi GHI đầu tiên, Toaster toàn site); **on-demand revalidation ✅ merge 03/08** (duyệt review → trang tour tươi ngay, nợ ADR-0016 trả xong); **bước 7 ✅ merge 03/08** (5/6 trang auth thật + `useSession` island + verify OTP — [ADR-0017](adr/0017-web-session-better-auth.md); two-factor PARK); **cụm A bước 8–10 ✅ merge 06/08** (hạ tầng session + 6 route account sống thật — static-first hai pha, user chốt T5); **bản đồ thật + gom nguồn liên hệ `/contact` ✅ merge 06/08** (MapLibre 2 marker thay `ImagePlaceholder`, `fitBounds` Hà Nội↔Hồ Chí Minh, `mocks/offices.ts` một nguồn thay 3 nguồn chỏi nhau, nút Get directions hết là link chết — [ADR-0018](adr/0018-web-map-library.md)); **cụm C booking/checkout ✅ merge 07/08** · **cụm B wishlist ✅ 10/08** · **ba vòng redesign khu account + checkout ✅ 10–11/08** (đỉnh là bản "Hộ chiếu", `bf20383`) · **bề mặt GHI media ✅ 12/08** (avatar + ảnh review qua Cloudinary signed upload) · **trùng tu Tour Details ✅ 13/08** (5 tab, xem dòng riêng bên dưới) · **Home đủ ảnh ✅ 14/08** (9/9 thẻ địa danh · 5/5 slider · 6/6 khối why-choose · cta-band; khe site lên 15). Cùng đợt đó phát hiện và vá **ba lời hứa sai** bằng cách đối chiếu chữ với DỮ LIỆU: "twelve max" sai ở 14 chỗ (19/30 tour vượt 12, cao nhất 22), "support 24/7" bị chính `mocks/offices.ts` phủ nhận (Mon–Fri 8–18), "free cancellation 48h" không có con số chung (15/30 tour tính bằng giờ). Bài học ghi lại: **lời hứa toàn cục về một con số thuộc dữ liệu từng tour thì sớm muộn cũng sai** — để số sống ở trang chi tiết. Huy hiệu tin cậy chuyển sang `trust-strip.tsx` · **ảnh /blog ✅ 17/08** (9/9 ảnh bìa bài viết; mở nhánh `posts/` cho ba script media — nhánh này KHÔNG nằm dưới địa danh vì một bài có thể nói về nhiều nơi hoặc không nơi nào, và KHÔNG có luật rơi-về. Vá một mắt xích thiếu: contract và schema đủ hai đầu nhưng view-model `JournalPost` bỏ rơi `cover` nên component không bao giờ thấy ảnh). **kế tiếp**: P4 admin theo roadmap CLAUDE.md — sổ nợ P3b còn mở gom ở [backlog 06/08](analysis/2026-08-06-backlog-no-ky-thuat.md) (ảnh tour là món chặn duy nhất) |
| P3b — trang Home | [2026-07-23-home-page-design](specs/2026-07-23-home-page-design.md) | ✅ đã merge (static-first, 33 vòng điều chỉnh) |
| P3b — cụm 6 trang auth | [2026-07-24-auth-pages-design](specs/2026-07-24-auth-pages-design.md) | ✅ trọn bộ 6/6 merge 25/07 (vé tàu + ảnh Sa Pa + topo mask + OTP + strength field); nợ wire API **đã trả 03/08** (bước 7 — 5/6 form, two-factor PARK theo [ADR-0017](adr/0017-web-session-better-auth.md) §5b) |
| P3b — cụm pháp lý/utility | [2026-07-25-legal-utility-pages-design](specs/2026-07-25-legal-utility-pages-design.md) | ✅ merge 25/07 — 4 trang nội dung dài + 3 route boundary; nợ robots/sitemap **đã trả 28/07** (cùng đợt 2 cụm Tours) — còn mở: EnquiryCta + API FAQ |
| P3b — cụm Blog | [2026-07-25-blog-pages-design](specs/2026-07-25-blog-pages-design.md) | ✅ merge 27/07 — /blog · /blog/[slug] · rss.xml; giữa chừng user đổi hướng 2 lần (gộp PostCard theo thiết kế Home · toàn site về placeholder); nợ tách ArticleBody + phân trang + API ghi trong spec — **cả 3 đã trả**: ArticleBody + phân trang 28/07 (`7a1e743`), nối API 31/07 (xem bước 1 nối API). ⚠️ **Spec tả thế giới mock** (9 bài `mocks/blog.ts`/journal) — mock đã khai tử ở bước 1 nối API 31/07, đọc [spec 31/07](specs/2026-07-31-blog-api-design.md) cho hiện trạng |
| P3b — cụm Tours | [2026-07-27-tours-pages-design](specs/2026-07-27-tours-pages-design.md) | ✅ merge 27/07 (đợt 1) + 28/07 (đợt 2 — robots + sitemap) — `/tours` duyệt sau **4 vòng thiết kế lại** · `/tours/[slug]` "Departure Board": chọn đợt đồng bộ **3 nơi** · robots + sitemap. §8 ghi **5 nợ contract** (media tour · next-departure trên card · sort rating · filter price/duration/difficulty · suitableFor+badges) — ĐỪNG vá trong cụm tĩnh; **chỉ #2–#5 cần ADR mới**, #1 đã được ADR-0005 chốt shape và hoãn có chủ đích. ⚠️ **Spec tả thế giới mock** (16 tour `mocks/tours.ts` theo contract) — mock đã khai tử trọn ở bước 2+3 nối API 31/07, đọc [spec 31/07](specs/2026-07-31-tours-catalogue-api-design.md) cho hiện trạng. Đợt **hình ảnh & uy tín 28/07** (card thiết kế lại · gallery · reviews) nằm **NGOÀI** spec này — user yêu cầu sau khi cụm đã đóng, ghi trong [CHANGELOG](CHANGELOG.md), không có spec riêng |
| P3b — cụm Destinations | [2026-07-28-destinations-pages-design](specs/2026-07-28-destinations-pages-design.md) | ✅ merge 30/07 — nhưng ⚠️ **spec này đã LỆCH bản đã ship**: nó đề xuất "tint chiếm trang", thứ [ADR-0015](adr/0015-retire-region-tint.md) đã rút bỏ TOÀN SITE sau khi user bác màu theo vùng. Bản thật phân hoá bằng **cấu trúc** (thứ tự khu · gallery 3 bố cục · trục chuyển động), không bằng màu. §4.2 vá khuyết tật **`tourCount` mock phồng 2–5×** (đã làm, lan sang `/about` 68 → 16 — số này chỉ đúng ở kỷ nguyên mock; catalogue thật từ bước 2+3 API là **30 tour**, đọc [spec 31/07](specs/2026-07-31-tours-catalogue-api-design.md)); §7 **cắt ≈202 dòng copy i18n** quảng cáo **4 địa danh v2 không bán** (Hà Giang 5 lần, Lan Hạ, Fansipan, Pù Luông = 0 trong mock — đã cắt). Đọc [CHANGELOG 30/07](CHANGELOG.md) để biết bản đã ship, đừng đọc spec như hiện trạng |
| P3b — nối API bước 1: cụm Blog + nền `lib/api` | [2026-07-31-blog-api-design](specs/2026-07-31-blog-api-design.md) | ✅ **merge 31/07** (`1cbe22c`, 13 commit) — nền `lib/api` theo [ADR-0016](adr/0016-web-data-layer.md) + seed 9 bài + 5 bề mặt đổi nguồn; nghiệm thu 8/8 §5 trên production build (slug lạ 404 thật); chip min-read bỏ khỏi card (contract card không có content); nợ mở ghi [CHANGELOG 31/07](CHANGELOG.md) |
| P3b — nối API bước 2+3: catalogue thật + `/tours` + detail | [2026-07-31-tours-catalogue-api-design](specs/2026-07-31-tours-catalogue-api-design.md) | ✅ **merge 31/07** (`5e80270`, 14 commit) — 30 tour + 19 dest + 84 review CURATED thay trọn seed cũ; `/tours` + detail + sitemap (52 URL) đọc API. ⚠️ Spec §5 departures ĐÃ AMEND (bản gốc tự mâu thuẫn); **bất biến rating ĐẢO** (mọi review approved có tourId đều tính — quyết định user, ghi [CHANGELOG 31/07](CHANGELOG.md)); ⚠️ trang vùng còn 14/16 link tour mock chết 404 → bước 4 phải đi ngay |
| P3b — nối API bước 4: cụm Destinations + xoá lệch mock | [2026-07-31-destinations-api-design](specs/2026-07-31-destinations-api-design.md) | ✅ **merge 01/08** (`30fe3f9`, 10 commit, net −1.028 dòng; docs sweep 03/08) — 24/24 link tour 200 toàn site, mocks catalogue khai tử trọn. ⚠️ Spec §1E/§4.3 ĐÃ AMEND (Task 6 navbar moot — premise sai, giữ thiết kế 4-link 30/07, user tái xác nhận 01/08); 3 quyết định hiển thị của user ghi [CHANGELOG 03/08](CHANGELOG.md) |
| P3b — đập-xây-lại khu Account "Hộ chiếu" | [2026-08-11-account-passport-redesign](specs/2026-08-11-account-passport-redesign.md) | 🔶 chờ user duyệt spec — user chọn archetype Hộ chiếu (demo vòng 2) rồi duyệt bộ 4 màn; route mới (`/account` = passport, settings tầng sau), contract thêm `tourDestinations`, hàm thuần tem/stats/bản đồ-chấm; giữ nguyên tầng logic đã test |
| P3b — trùng tu Tour Details | [2026-08-13-tour-detail-redesign](specs/2026-08-13-tour-detail-redesign.md) · [wireframe nguồn](design/mockups/tour-detail.src.html) · [wireframe tab Departures](design/mockups/tour-detail-departures.src.html) · [ADR-0022](adr/0022-tour-detail-tabs.md) | ✅ **merge 13/08** — trang chuyển sang **5 tab** (Overview · Itinerary · Departures · Reviews · Good to know); `OnThisPage` rời trang này, tab bar thay vai mục lục. ⚠️ **Nhánh chứa HAI đợt thi công**: đợt đầu dựng theo spec ghi sai số đo (1104/621/541 đo từ bản ReUI gốc thay vì 1056/573/493 của wireframe đã duyệt) và thêm một dải khởi hành bản duyệt không có — đã **xoá trọn phần thân** rồi dựng lại bám thẳng markup wireframe. [Plan cũ](plans/2026-08-13-tour-detail-redesign.md) đã khai tử (Task 1–3 vẫn đúng); spec hiện hành chép số đo trích bằng máy. Tab Departures là chỗ DUY NHẤT cố ý khác bản duyệt (dải khối ngang không đọc được khi thử người dùng → bảng nhóm theo tháng có hàng xổ, spec §2.6). Nghiệm thu: **0 lệch** trên 38 phần tử × 2 chế độ màu, 28/28 hành vi, 0 lỗi console, 74/74 trang tĩnh (spec §6). **Sổ nợ đã trả 14/08** (nhánh `feat/tour-content-debt`, [ADR-0023](adr/0023-tour-merchandising-fields.md)): mô tả 4 card dữ kiện · tiêu đề 90 thẻ policy · `freeCancellationDays` · thu/phóng lightbox, cộng **ba thẻ chính sách cuối tab Departures bị bỏ sót ở R6** (bộ so R9 mù với phần tử app THIẾU HẲN — chỉ phép đếm khối theo pane mới bắt được). Còn đúng một món: **ảnh tour** — cần user duyệt bằng mắt TRƯỚC upload theo [ADR-0020 bản sửa](adr/0020-real-images-sourcing.md) nên không uỷ quyền được |
| P3b — `/contact` §2 gộp về một card + panel ảnh | [wireframe ReUI contact-2](design/mockups/contact-reui-2.src.html) · [wireframe ReUI contact-5](design/mockups/contact-reui-5.src.html) · [wireframe bản ghép đã duyệt](design/mockups/contact-split-panel.src.html) | ✅ **merge 17/08** — không có spec riêng: đây là vòng thiết kế chạy bằng wireframe, ba file mockup CHÍNH LÀ bản ghi. Hai card rời (bản sáng 17/08) bị user bác *"nhìn cứ kỳ kỳ"*; cân kích cỡ hai khung giải sai bài toán — vấn đề là có HAI khung, không phải hai khung lệch. Chốt theo ReUI contact-2: MỘT card chia đôi, nửa trái là panel **tràn viền**; hàng thông tin có icon lấy của contact-5; đảo vế (panel nằm TRÁI) để giữ thứ tự đọc sẵn có. Cùng đợt: dropdown vùng → **4 radio** 2×2 (thẻ cao 731→792), **chữ ký tự điền** tên khách đã đăng nhập (cờ `filledOnce` chống ghi đè vì `useSession` trả bất đồng bộ), khe site lên **16** (`contact-panel`). Số đo wireframe trích bằng `getComputedStyle` trên trang gốc, không ước lượng. Ảnh panel là bản đồ Mỹ — nêu ra, **user cân nhắc và chốt giữ**; xem [CHANGELOG 17/08](CHANGELOG.md) |
| P3b — ảnh `/about`: Story · Gallery · Timeline · đội ngũ | — (không có spec riêng) | ✅ **merge 17/08** — `/about` từ 1 ảnh thật xuống còn **2 ô giữ chỗ** (nền Numbers, video CTA); khe site lên **28**. Mở 12 khe mới: `about-gallery-*` ×4 · `about-timeline-*` ×4 · `about-team-*` ×4 (khoá team đặt theo CHỨC DANH chứ không theo tên, để đổi người không phải đổi khe). ⚠ **Vá một lỗi định vị ẩn kỹ ở `SlotImage`**: hai nhánh của nó không giống nhau — nhánh giữ chỗ tự mang `relative overflow-hidden`, nhánh ảnh thật thì không, nên `next/image fill` thoát ra bám viewport; lỗi chỉ vỡ ĐÚNG LÚC khe được gắn ảnh thật (§Team: ô 302×320 mà ảnh render 1440×900). ⚠ **Đính chính số đo panel `/contact` của entry 17/08 trước đó** — `context.newPage()` của Playwright không nhận `{viewport}` nên mọi lần đo đều chạy ở 1280×720; tỉ lệ thật trải 0.36→0.81. Đội ngũ chuyển sang người thật + avatar DiceBear `voxel-bot` (CC0, API 10.x); hover đổi chiều thành xám→bừng màu, bọc `[@media(hover:hover)]` để mobile không xám vĩnh viễn. Chi tiết và sổ nợ ở [CHANGELOG 17/08](CHANGELOG.md) |
| P3b — video nền dải CTA `/about` + đường ống media nhận VIDEO | — (không có spec riêng) | ✅ **merge 17/08** — khe **VIDEO đầu tiên** của dự án (`about-cta-video`), khe site lên **29**; `/about` HẾT ô giữ chỗ (18 ảnh + 1 video). Contract và `buildCloudinaryUrl` vốn đã hỗ trợ video từ ADR-0005, chỉ `media-scan`/`media-upload` còn mù — kiểu nợ nằm im lâu vì từng mảnh đều 'xong', chỉ thiếu khúc nối. `SlotVideo` là component RIÊNG (không nhét nhánh vào `SlotImage`) — rút thẳng bài học từ lỗi định vị cùng ngày. ⚠ Video tự chặn cỡ `w_1600,c_limit` vì không đi qua `next/image`: **91MB → 4,4MB** trên clip thật. Khối Numbers CHỐT không gắn ảnh (ô giữ chỗ cũ chỉ hiện ~9% dưới scrim) nên KHÔNG mở khe `about-numbers`. Chi tiết, ba review finding và sổ nợ ở [CHANGELOG 17/08](CHANGELOG.md) |
| P3b — redesign Checkout (hướng B) + khu Account (hướng A) | [2026-08-10-checkout-account-redesign](specs/2026-08-10-checkout-account-redesign.md) | ✅ **merge 11/08** (`330452e..05d4d1c`, 27 commit, 66 file) — checkout USER DUYỆT (vé boarding-pass theo giải phẫu thật, DatePicker shadcn, badge outline, Reserve nối dây); ⚠️ khu account thi công đúng spec nhưng **user bác visual** — vòng đập-xây-lại đang chạy (demo vòng 2: 3 kiến trúc từ-số-0, chờ user chấm); chi tiết + sổ nợ ở [CHANGELOG 11/08](CHANGELOG.md) |
| P3b — bước 8–10 cụm A: hạ tầng session + khu Account | [2026-08-04-account-area-design](specs/2026-08-04-account-area-design.md) | ✅ **merge 06/08** (`2cebb8b`, 12 commit, 51 file) — static-first HAI PHA đúng mốc: user chốt visual T5 (mức dựng-tạm, redesign session sau) rồi mới wire; 6 route + hạ tầng session ADR-0017 trọn; 4 bug thật bị review/smoke bắt (CORS DELETE · proxy `__Secure-` · link chết unavailable · lỗ test orderBy). ⚠️ Spec AMENDED 06/08 (byCode+cancellationStatus); sổ nợ trong [CHANGELOG 06/08](CHANGELOG.md). Booking (cụm C) chờ user chốt design — prompt tại [design/prompts/booking.md](design/prompts/booking.md) |
| P3b — cụm C: booking form + checkout | [2026-08-07-booking-checkout-design](specs/2026-08-07-booking-checkout-design.md) | ✅ **merge 08/08** (`1d58935`, ff-only, 8 commit `13bc78e..1d58935`) — 3 route mới; ngõ cụt 404 ở money-path đã đóng, nghiệm thu SỐNG đi trọn vòng qua Stripe sandbox (`BK-C15SAN5V` PAID, webhook 5×200, ghế claim đúng invariant). Contract nở 3 field additive; KHÔNG mở `decisionNote` cho khách. 3 bug bị bắt trong lúc làm — nặng nhất là thiếu `noValidate` khiến validate của mình không bao giờ chạy, xem [CHANGELOG 07/08](CHANGELOG.md). Nợ mở: chưa đo đường PayPal trên UI |
| P3b — redesign khu account (nợ A1+A2+A3) | [2026-08-08-account-redesign-design](specs/2026-08-08-account-redesign-design.md) | ✅ **xong 10/08** ([plan](plans/2026-08-10-account-redesign.md) 34/36 mục — 2 mục KHÔNG làm, có ghi lý do). Ba nợ đóng cùng lượt. A3 sâu hơn sổ ghi: sổ áp sai ngưỡng (2.91/2.57 là cặp BỀ MẶT 3:1, không phải chữ 4.5:1) và bỏ sót nhóm tệ nhất — `text-primary` trên `muted` đo **2.05**, nay 5.01. A2 không phải nợ thẩm mỹ: chuỗi hardcode đang được email NGƯỢC cho chính khách. Cụm B đóng trọn bằng một field additive `reviewedAt`. **Ba lỗi chỉ ẢNH CHỤP bắt được** trong khi test đều xanh: "Trips booked 0" mâu thuẫn với ba dòng PENDING ngay dưới · nhãn trường lặp hai lần ở trang hồ sơ · navbar TÀNG HÌNH ở chế độ sáng trên 8 trang không có hero (lỗi có sẵn, vá ở `b7ccf83`). Kèm hai lỗi có sẵn khác: lỗi trong dialog render sau lớp modal (bàn phím không với tới link đăng nhập lại), và `SavedGrid` thiếu nhánh 401 |
| P3b — khu account vòng 2: lưới ba toạ độ + hub khối | *(không có spec — làm trực tiếp theo mẫu user gửi, 3 vòng bác/dựng lại)* | ✅ **merge 11/08** (`a471711`, ff-only, 3 commit). Gốc lệch lề sâu hơn chỗ nhìn thấy: 52 chỗ trên site dùng `lg:px-24 xl:px-32`, khu account là chỗ DUY NHẤT dùng `px-16` — do commit sidebar (đã gỡ) cắt padding lấy chỗ cho cột dọc. Nay cả khu chỉ có BA toạ độ x (128 · 536 · 1312), giữ bằng MỘT khai báo lưới dùng chung cho năm màn (`account-section.tsx`). `/account` thành hub khối theo mẫu Airbnb; trang trong là dòng dữ liệu trong card. Bắt một lỗi WCAG thật: pill `bg-warning/10 text-warning` đo **1.90:1** ở chế độ sáng. `destructive` mắc đúng mâu thuẫn hai vai của [ADR-0019](adr/0019-color-token-roles.md) → tách `destructive-emphasis` (mục 2b). ⚠️ **Đây KHÔNG phải bản chốt thẩm mỹ** — user đánh giá kết quả là chắp vá và chốt giao việc thiết kế lại toàn bộ khu account cho session khác dựng LẠI TỪ ĐẦU; bản này là nền để đập đi xây lại. Khu checkout/booking chưa đụng tới |
| P3b — bản đồ thật `/contact` + gom nguồn liên hệ | [2026-08-06-contact-map-offices-design](specs/2026-08-06-contact-map-offices-design.md) | ✅ **merge 06/08** (`76720c5`, 11 commit, [plan](plans/2026-08-06-contact-map-offices.md) 6/6 task) — đối chiếu Nexora lộ **3 nguồn thông tin liên hệ chỏi nhau cùng live** (`mocks/offices` HN+Sa Pa · hardcode topbar/split · i18n HN+HCM in trên `/terms`+`/privacy`) và **2 thụt lùi chưa từng ghi nhận** (map là `ImagePlaceholder`; nút Get directions `href="#visit"` là link chết). Đã ship: địa chỉ theo Nexora (HN 18 Tam Trinh + HCM 184 Lê Đại Hành — đều là cơ sở VTC Academy), `mocks/offices.ts` làm nguồn duy nhất (đúng [ADR-0016](adr/0016-web-data-layer.md)), MapLibre 2 marker + `fitBounds`. Toạ độ geocode từ OSM (**không** port toạ độ Nexora — lệch ~600m); điện thoại **giữ** `+84 24 3826 0126` vì `1900 292 958` là hotline thật của VTC Academy. [ADR-0018](adr/0018-web-map-library.md) đi trước code đúng luật #5. ⚠️ Spec §4a và §6 ĐÃ AMEND (viết riêng component thay vì port primitive; top-bar dùng `lib/site.ts` thay vì import từ `home/contact` — lý do RSC/`'use client'` boundary); 4 phát hiện review ghi ở [CHANGELOG 06/08](CHANGELOG.md) |
| Trả nợ ADR-0002: PayPal capture + smoke thật | [2026-08-04-paypal-capture-smoke-design](specs/2026-08-04-paypal-capture-smoke-design.md) | ✅ **merge 04/08** (`9aa338f`, 5 commit) — PayPal end-to-end lần đầu (capture-on-approved sống 23s trên sandbox); smoke thật 2 provider + refund 2 nhịp; 3 bug lớp chỉ-lộ-khi-chạm-đời-thật bắt+vá (details[].issue · Stripe expiry 60′ · TTL sweep 65′ với unit khoá bất biến) — [CHANGELOG 04/08](CHANGELOG.md); [plan](plans/2026-08-04-paypal-capture-smoke.md) 3 task |
| P3b — nối API bước 7: 6 trang auth + session | [2026-08-03-auth-pages-api-design](specs/2026-08-03-auth-pages-api-design.md) | ✅ **merge 03/08** (`9a0c30a`, 7 commit) — 5/6 form thật ([ADR-0017](adr/0017-web-session-better-auth.md)); verify email chuyển OTP (SEC-1 đo sống 2 chiều); `mocks/auth.ts` khai tử; two-factor PARK. ⚠️ Spec §8.5 ĐÃ AMEND (`/tours` listing vốn dynamic — ISR đo ở `[slug]`); gotcha defu ghi [CHANGELOG 03/08](CHANGELOG.md) |
| P3b — on-demand revalidation (trả nợ ADR-0016) | [2026-08-03-on-demand-revalidation-design](specs/2026-08-03-on-demand-revalidation-design.md) | ✅ **merge 03/08** (`6be5abe`, 5 commit) — route web `/api/revalidate` + `WebRevalidationService` móc `reviews.moderate` sau commit; hard-bust `{expire: 0}` (không phải `'max'` SWR — bài học Next 16, xem [CHANGELOG 03/08](CHANGELOG.md)). ⚠️ Spec §4 ĐÃ AMEND (dùng lại `FRONTEND_URL`, không thêm `WEB_URL`) |
| P3b — nối API bước 5+6: form Contact + Newsletter + unsubscribe | [2026-08-03-contact-newsletter-api-design](specs/2026-08-03-contact-newsletter-api-design.md) | ✅ **merge 03/08** (`5afddf8`, 6 commit) — site có hành vi GHI đầu tiên; Toaster sonner toàn site (quyết định user); nghiệm thu 5/5 trên DB thật (429 throttle + vòng token HMAC). ⚠️ Spec §4/§7.3 ĐÃ AMEND (2 mâu thuẫn nội bộ — bài học spec-writing thứ 3+4, xem [CHANGELOG 03/08](CHANGELOG.md)) |
| P4 Admin · P5 Mobile · P6 AI · P7 Polish UI | — | ⬜ chưa mở |

## Plans — kế hoạch triển khai (task-by-task)

| Kế hoạch | Phủ | Trạng thái |
| --- | --- | --- |
| [P3a-A: Nền chung + Reviews](plans/2026-07-19-p3a-a-foundation-reviews.md) | W0 + W1 (6 task) | ✅ đã merge |
| [P3a-B: Wishlist · Enquiry · Newsletter](plans/2026-07-19-p3a-b-wishlist-enquiry-newsletter.md) | W2–W4 + hạ tầng rate limiting (6 task) | ✅ đã merge |
| [P3a-C: Posts · Site-media](plans/2026-07-21-p3a-c-posts-site-media.md) | W5–W6 + hạ tầng media-đọc (7 task) | ✅ đã merge |
| [Admin bootstrap emailVerified + AUTH-2](plans/2026-07-21-admin-bootstrap-verified.md) | SEC-1/AUTH-1/AUTH-2 (5 task) | ✅ đã merge |
| [Refund correctness](plans/2026-07-21-refund-correctness.md) | BK-R1/PAY-R1/TOCTOU (5 task) | ✅ đã merge |
| [P3a contract closeout](plans/2026-07-21-p3a-contract-closeout.md) | C1·R1·R2 parity (4 task) | ✅ đã merge |
| [Vòng đời PENDING](plans/2026-07-22-pending-lifecycle.md) | BK-1·BK-2·PAY-1·WRK-1 (5 task) | ✅ đã merge |
| [Typeset trong @tourism/ui](plans/2026-07-22-ui-typeset.md) | ADR-0012 (3 task) | ✅ đã merge |
| [Theme Wuling + region tokens](plans/2026-07-22-wuling-theme-tokens.md) | ADR-0013 (5 task) | ✅ đã merge |
| [Trang Home tĩnh](plans/2026-07-23-home-page.md) | Shell + mocks + 8 section gốc (sau thành 10 qua review) | ✅ đã merge |
| [Cụm 6 trang auth](plans/2026-07-24-auth-pages.md) | Route group + AuthScreen/TicketCard + 6 trang (6 task) | ✅ đã merge trọn (Task 1–2 ngày 24/07 · Task 3–6 ngày 25/07) |
| [Cụm pháp lý/utility](plans/2026-07-25-legal-utility-pages.md) | i18n + LegalArticle + 4 trang + 3 boundary (7 task) | ✅ đã merge |
| [Cụm Blog](plans/2026-07-25-blog-pages.md) | mock 9 bài + lib/blog + 3 route + RSS (7 task + 3 đợt vá) | ✅ đã merge |
| [Cụm Tours](plans/2026-07-27-tours-pages.md) | mock 16 tour theo contract + lib/tours + `/tours` + `/tours/[slug]` + robots/sitemap + 4 khoản nợ (13 task) | ✅ đã merge (đợt 1 27/07 · đợt 2 28/07). Có **bảng 4 vòng thiết kế lại** của listing; bẫy soft 404 do `loading.tsx` đo lần đầu ở đây, nay tách thành convention [`soft-404-loading-tsx`](conventions/soft-404-loading-tsx.md) (đọc trước khi thêm `loading.tsx` vào bất kỳ route động nào) |
| [Account "Hộ chiếu" đập-xây-lại](plans/2026-08-11-account-passport.md) | 10 task theo [spec 11/08](specs/2026-08-11-account-passport-redesign.md): T1 token paper/ink → T2 contract tourDestinations → T3 hàm thuần (stats/tem/mrz/map) → T4 bộ component passport → T5 trang `/account` M1+M4 + route lại → T6 settings → T7 trang visa → T8 saved → T9 nghiệm thu worktree | 🔶 chờ thi công |
| [Redesign Checkout B + Account A](plans/2026-08-10-checkout-account-redesign.md) | 11 task theo [spec 10/08](specs/2026-08-10-checkout-account-redesign.md): T1 contract thêm tourSlug/tourImage → T2–T4 checkout B (summary card, trang book 2 cột, success voucher-first) → T5–T9 account A (3 tab, TripCard, detail, profile nở-inline, saved) → T10 đóng nợ contrast bằng viền → T11 nghiệm thu worktree | ✅ **merge 11/08** — 11/11 + final review (opus) + 6 vòng polish theo user; lỗi PLAN bị final review bắt (badge sai policy, order-first mobile, H1 3xl) ghi trong CHANGELOG |
| [Cụm A: hạ tầng session + khu Account](plans/2026-08-04-account-area.md) | 8 task HAI PHA theo [spec 04/08](specs/2026-08-04-account-area-design.md): session client → VM+mock → 6 route tĩnh → MỐC DỪNG T5 user chốt → byCode+cancellationStatus (6a AMENDED) → wire đọc → wire hành động → nghiệm thu worktree riêng | ✅ **merge 06/08**, 8/8 + 3 vòng fix (2 review + 1 final); T5 có 2 fix từ vòng test thật của user; sự cố build-đè-dev-server ghi bài học vào memory + brief |
| [Bước 7: 6 trang auth + session](plans/2026-08-03-auth-pages-api.md) | 7 task theo [spec 03/08](specs/2026-08-03-auth-pages-api-design.md): API emailOTP + SEC-1 int (điều khoản DỪNG) → nền client (safeRedirect/mapAuthError TDD) → login+register → forgot+reset → verify-OTP + PARK 2FA → user-menu → nghiệm thu sống 6 mục | ✅ **merge 03/08**, 7/7 task + 1 vòng fix final (bug pending-kẹt xuyên 4 form — 6 task-review lọt, final bắt); Task 1 AMENDED (gotcha defu) |
| [On-demand revalidation](plans/2026-08-03-on-demand-revalidation.md) | 4 task theo [spec 03/08](specs/2026-08-03-on-demand-revalidation-design.md): lõi thuần + route web → env + service API + hàm quyết định → móc moderate sau commit (thân tx +1 dòng) → nghiệm thu sống 5 mục | ✅ **merge 03/08**, 4/4 task + 1 vòng fix (reviewer T1 bắt `'max'` là SWR mềm → `{expire: 0}` hard-bust; reviewer T3 mutation-bite điều kiện bust) |
| [Bước 5+6: contact + newsletter + unsubscribe](plans/2026-08-03-contact-newsletter-api.md) | 5 task theo [spec 03/08](specs/2026-08-03-contact-newsletter-api-design.md): i18n+Toaster+classifySubmitError → form contact (buildEnquiryPayload TDD, honeypot) → newsletter footer → trang unsubscribe 4 trạng thái → nghiệm thu DB thật + 429 throttle + z-index toast | ✅ **merge 03/08**, 5/5 task + 2 vòng fix (final review bắt bug dates-vô-hiệu-hoá-loves cả task review lọt) |
| [Redesign khu account](plans/2026-08-10-account-redesign.md) | 9 task theo [spec 08/08](specs/2026-08-08-account-redesign-design.md): chuẩn bị test-safety → token ADR-0019 → 5 màn → cụm B nửa 2 → nghiệm thu. Thứ tự CỐ Ý đặt token trước markup: đổi token là việc TOÀN SITE nên phải chạy `gate` riêng để bắt chỗ vỡ ngoài phạm vi | ✅ **xong 10/08**, 34/36 mục. Hai mục không làm có ghi lý do: tách 5 hook (mục đích là cứu test khỏi bám DOM, nhưng markup đổi thật nên spec phải viết lại bằng mọi giá — hook chỉ thêm tầng gián tiếp) và dựng slot review rỗng (gộp vào Task 8 thay vì dựng rồi thay ngay). Task 7 làm MẠNH hơn plan: gỡ hẳn `wishlist-vm` thay vì chỉ giấu phần bịa — rẻ đi nhờ Task 3 đã bỏ consumer thứ hai |
| [Bản đồ thật + gom nguồn liên hệ `/contact`](plans/2026-08-06-contact-map-offices.md) | 6 task theo [spec 06/08](specs/2026-08-06-contact-map-offices-design.md): ADR-0018 → mock `OFFICES` một nguồn → hook theme + `contact-map.tsx` → lắp map lười vào §Location + vá link chết → gom nguồn liên hệ/xoá mồ côi i18n → nghiệm thu sống + docs sweep | ✅ **merge 06/08** (11 commit `98d2800..76720c5`), 6/6 task — 4 phát hiện review đều là lỗi PLAN, không phải lỗi triển khai (theme kẹt dark→light · stub `IntersectionObserver` global sai quy ước đã đo · import hằng RSC vỡ prerender · test khung toạ độ không bắt hoán đổi chéo); xem [CHANGELOG 06/08](CHANGELOG.md). Docs sweep sửa lại 3 chỗ spec/plan đã lệch code (top-bar dùng `lib/site.ts` chứ không phải import từ `home/contact`; stub `IntersectionObserver` cục bộ chứ không phải global; viết riêng component thay vì port primitive) |
| [Bước 4: cụm Destinations lên API + xoá lệch mock](plans/2026-07-31-destinations-api.md) | 7 task theo [spec 31/07](specs/2026-07-31-destinations-api-design.md): lib/regions sang VM → /destinations → 3 trang vùng (reviews compose per-tour) → Home + moments hết link chết → /about → navbar 19 điểm (⚠️ layout fetch — có điều khoản DỪNG) → khai tử mocks/tours + destinations + nghiệm thu "0 link tour chết toàn site" | ✅ **merge 01/08** (docs sweep 03/08), 6/7 task + T6 moot; 3 vòng fix đều là lớp "số thật vs số bịa"; final review bắt 2 seam xuyên trang |
| [Bước 2+3 nối API: catalogue thật + /tours + detail](plans/2026-07-31-tours-catalogue-api.md) | 10 task theo [spec 31/07](specs/2026-07-31-tours-catalogue-api-design.md): tách fixtures theo miền → content 3 miền → reviews + recompute → reset DB + đo → lib/api/tours → listing → detail → sitemap + nghiệm thu | ✅ **merge 31/07**, 10/10 task + 4 vòng fix (2 do spec tự mâu thuẫn — bài học ghi CHANGELOG); 3 vòng review content độc lập; final review bắt 2 lỗi xuyên-trang task-review không thấy được |
| [Bước 1 nối API: cụm Blog + nền lib/api](plans/2026-07-31-blog-api.md) | 10 task theo [spec 31/07](specs/2026-07-31-blog-api-design.md): deps+env → client+tags → tri-state → seed 9 bài (⚠️ bảng dời 4 ngày mock ở tương lai về quá khứ — ADR-0004 lọc `publishedAt <= now`) → VM+fetch → markdown/TOC → 2 trang + rss + sitemap + teaser Home → khai tử mock journal + nghiệm thu | ✅ **merge 31/07**, 10/10 task + 2 vòng fix (2 bug đều từ code mẫu trong plan — bài học ghi CHANGELOG); thi công subagent-driven, review độc lập từng task |
| [Cụm Destinations](plans/2026-07-28-destinations-pages.md) | token `--region-hero` + đắp lại mock gương contract + `lib/regions` + 2 trang + nối dây nav/sitemap; nở từ 7 task thành **Task 1–7 cộng 4b và 5b–5o** (~3.900 dòng) | ✅ **ĐÓNG CỤM 30/07.** Task 1–5 và toàn bộ 4b, 5b–5o (2 lần dựng lại `/destinations`, 4 lần dựng lại trang vùng), rồi **Task 6** (sitemap 38 URL — thêm `/destinations` 0.9 và 3 URL vùng 0.8, cùng nguồn `REGIONS` với `generateStaticParams`) và **Task 7** (đo trên production build: slug lạ → **404 thật**, không soft-404) đóng ở `cf8f821`. Có **2 mốc dừng bắt buộc**, lý lẽ vì sao **KHÔNG** dùng `color-mix` cho hero vùng, và mục **4 thứ pre-flight bắt được** — trong đó việc sửa `tourCount` lan sang `/about` (68 → 16 tour) |

## Analysis — nghiên cứu từ Nexora

| Tài liệu | Dùng để |
| --- | --- |
| [Schema audit](analysis/2026-07-18-schema-audit-nexora.md) | Soi 27 model + quyết định tối ưu (H/M/LOW) |
| [API parity + upgrade map](analysis/2026-07-19-api-parity-upgrade-map.md) | Kiểm kê ~64 endpoint còn thiếu + 14 nâng cấp + 10 delta schema |
| [Infra parity](analysis/2026-07-19-infra-parity-nexora.md) | 8 lỗ hạ tầng xuyên suốt — thứ API parity map bỏ lọt |
| [Quét sâu Nexora](analysis/2026-07-19-nexora-deep-sweep.md) | **Bảng theo dõi A1–A11** + quy tắc nghiệp vụ W2–W6 + kiến trúc P3b |
| [Kiểm kê env keys](analysis/2026-07-19-env-keys-inventory.md) | Key nào đã lấy/còn thiếu, rủi ro ngày bảo vệ |
| [Đối chiếu lại P3a-B](analysis/2026-07-21-p3a-b-parity-recheck.md) | Rà song song wishlist·enquiry·newsletter — không thụt lùi Quan trọng, chỉ 2 điểm Nhỏ |
| [Rà soát độc lập toàn API](analysis/2026-07-21-independent-review.md) | **Parity + review defect toàn `apps/api`** — 4 High (refund/spam), chùm Medium, 19 invariant canh mạnh; tiền-RA là điểm yếu |
| [Sweep parity toàn code trước P3a-B](analysis/2026-07-21-full-parity-sweep-pre-p3ab.md) | Đối chiếu parity 7 vùng (catalog·reviews·bookings·payments·cancel/refund·auth·worker), 7 agent — 1 Quan trọng (C1 catalog destination phụ) + 4 Nên có + 3 Nhỏ + nợ; invariant money/security lõi sạch. *Snapshot 21/07; B2·C3 đã vá sau (CAT-4/BK-3)* |
| [Đối chiếu Nexora — cụm Tours](analysis/2026-07-27-tours-parity-nexora.md) | Hai tầng (endpoint/feature + hạ tầng xuyên suốt) cho listing & detail. Nexora **hardcode `departures: []`** nên khối chọn ngày của họ luôn ẩn — v2 có dữ liệu thật, đây là điểm nhấn số 1 của trang detail. Kèm 7 thụt lùi Quan trọng (robots/sitemap · JSON-LD Product · cache-tag revalidate · next-departure trên card…). ⚠️ **Snapshot lịch sử 27/07 — đã lỗi thời một phần**: chụp lúc `/tours` còn đọc mock, một số thụt lùi (robots/sitemap) đã vá 28/07, phần khác đã đổi sau khi nối API 31/07 |
| [Đối chiếu Nexora — tầng dữ liệu web](analysis/2026-07-31-web-data-layer-parity-nexora.md) | **Nền của [ADR-0016](adr/0016-web-data-layer.md)** — Nexora dùng openapi-fetch codegen + ISR 300s/tag + on-demand revalidation + `settle()`/`LoadErrorState`; bảng phân loại 18 hạng mục (thụt lùi duy nhất: on-demand revalidation — nợ có kế hoạch), 4 bài học giữ nguyên văn, các mẫu để dành cho bước 8–10/P5/P6 |
| [Rà soát docs ↔ code + tiến độ (trước phase nối API)](analysis/2026-07-30-docs-audit-progress.md) | **Chụp tại `5c81549`, đọc TRƯỚC khi viết dòng API đầu tiên.** 19 trang web đã dựng nhưng **0 trang gọi API**, và `apps/web` **chưa có API client nào** (không `@tourism/contract`, không `@orpc/client`) → nối trang đầu là quyết định kiến trúc cho cả 19 trang, cần ADR (luật 5). Kèm **thứ tự nối API 10 bước** đơn giản → phức tạp, **4 mock không có endpoint** (faq · testimonials · moments · regions), **16 procedure đã có mà chưa UI nào dùng**, và bảng doc-lệch-code. ⚠️ **Snapshot lịch sử 30/07 — đã lỗi thời một phần**: "0 trang gọi API" chỉ đúng tới trước bước 1 nối API (31/07); từ đó nhiều trang đã đọc API thật, xem [ADR-0016](adr/0016-web-data-layer.md) + các dòng "bước N nối API" ở bảng Specs |
| [Độ sẵn sàng backend + đối chiếu Nexora (trước P3b)](analysis/2026-07-22-backend-readiness-vs-nexora.md) | **Go/No-Go cho web** — 4 agent (endpoint·hạ tầng·money-path·auth/chất lượng). Backend P1-P3a xong & vượt Nexora ở tầng lõi; còn cụm PENDING-lifecycle (ADR-0006) + 3 infra-TB + P4-admin (58 ep, phase sau). Web không bị chặn cứng |
| [Báo cáo tiến độ 04/08](analysis/2026-08-04-progress-report.md) | **Mốc trước bước 8–10** — đã làm (33 endpoint · 20 route · 1.212 test) / còn lại (bước 8–10, P4 admin 38 trang, P5, P6, sổ nợ phân nhóm) / Nexora↔v2 (8 mảng v2 hơn · thụt lùi còn mở · chưa-tới-lượt). Nợ ADR-0002 xếp TRƯỚC bước 10 |
| [**Sổ nợ kỹ thuật còn mở** (sống, cập nhật liên tục)](analysis/2026-08-06-backlog-no-ky-thuat.md) | **Đọc TRƯỚC khi mở cụm việc mới, và BẮT BUỘC trước session redesign giao diện.** Khác các dòng còn lại trong bảng này (đều là ảnh chụp một thời điểm), file này là sổ SỐNG — trả nợ thì gạch bỏ tại chỗ kèm hash, không xoá. Gom nợ vốn nằm rải trong "Nợ mở" của từng entry CHANGELOG, chia 5 nhóm: **A** ảnh hưởng trực tiếp tới redesign (khu account dựng-tạm chờ thiết kế lại · textarea lý do huỷ · tương phản `primary` dark 2.91/2.57 dưới ngưỡng 4.5:1 · booking chờ chốt design · con dấu "Sa Pa" mồ côi user đã chốt GIỮ) · **B** dọn dữ liệu (khối `contact.*` i18n mồ côi trọn) · **C** lỗ kiểm thử (không gì canh việc gom nguồn liên hệ) · **D** thư viện (`input-otp` rò rỉ timer — đang giảm thiểu, chưa sửa gốc, đã làm đỏ CI 06/08) · **E** nợ cũ vẫn mở |
| [Đại tu docs 03/08](analysis/2026-08-03-docs-overhaul-audit.md) | Audit đối chiếu docs ↔ code toàn cục (3 auditor song song: ADR · bản đồ+specs/plans+conventions+skills · CHANGELOG+file dài); nền của đợt sửa README/conventions/skills.md này và của việc tách CHANGELOG + tách Soft-404 |

## Conventions — luật áp dụng mãi

| Tài liệu | Nội dung |
| --- | --- |
| [booking-states](conventions/booking-states.md) | Ledger kể chuyện tiền, status kể chuyện ghế — 5 ngữ nghĩa terminal |
| [outbox-dedupe-key](conventions/outbox-dedupe-key.md) | `<event>:<entityId>[:<state>]` — chống bug nuốt email 16/07 |
| [read-then-write-races](conventions/read-then-write-races.md) | Bẫy EvalPlanQual — đã cắn dự án 2 lần, kèm cách sai đã thử |
| [color-system](conventions/color-system.md) | Hệ màu brand Wuling + 3 vùng: nguồn cảm hứng (Endfield — codename nội bộ), số đo raw → giá trị chốt, tint vùng (đã rút — xem ADR-0015), pháp lý |
| [soft-404-loading-tsx](conventions/soft-404-loading-tsx.md) | `loading.tsx` trên route động + `notFound()` → HTTP 200 kèm UI 404; luật, cách đo, tách từ plan cụm Tours |

## Quy tắc viết

- **ADR trước code**; spec được user duyệt trước khi triển khai.
- **CHANGELOG là nơi duy nhất** giữ lịch sử và tiến trình số test.
- Doc hiện-trạng giữ NGẮN, chỉ phản ánh hiện tại; chuyện đã qua để CHANGELOG lo.
- Spec của skill `superpowers:brainstorming` cũng ghi vào `specs/` — **không tạo
  `docs/superpowers/`** (skill cho phép override đường dẫn mặc định).
